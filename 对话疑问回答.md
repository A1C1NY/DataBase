1. 双 API 的 ID 映射风险
   我的设计中有说明数据库内对车站是考虑单独顺序编号作为表内标识符号的。两个API的ID是可以空缺的。
2。 ArrivalInfo 的“当前车/下一辆车”
    这个是由于API只会返回最近的2辆车，所以写死了。为保证建表和连接简单，我们还是保留现在的横向设计
3. 为 3NF 拆出 Vehicle，实际收益较小
    我决定听从你的建议，将其重新合并回ArrivalInfo
4.  首末班时间字段设计不准确
    听从你的建议
5.  DispatchCar.time 只有时间，没有日期
    使用你的方法，即plannedDepartureAt DATETIME NULL
6. Line 的重复信息
    使用你的方法：Line.direction 保留，作为该线路方向的正式属性。 ArrivalInfo.upDown，仅作为保存原始 API 值。起终点最好通过 LineRoute 的第一站和最后一站得到。
7. 暂时还是使用B+树，我们实验中不会超过80车站
8. “站点查询热度”“预测候车时间趋势”“计划发车间隔”“无障碍车辆观测比例”，确实应该这样说明
9. IngestionRun这张表应该加入，使得这些信息可以保留
10. 顺序作出回答：
    QueryLog.userId 可空，用户删除后置空
    线路和站点不物理删除
    API 返回字段大部分允许 NULL。
    给快照添加防重复唯一约束