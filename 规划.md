# 高德公交站点与线路查询分析系统新版实施规划

## 1. 项目目标与硬性边界

本文件是新项目的唯一业务、数据库和技术依据。旧项目的上海市政府 API、实时到站、车辆、发车计划、固定坐标定时采集和相关表全部废弃，不做兼容字段，不做数据迁移。

系统只使用高德公交 Web Service：

- `v3/bus/stopname`：按站点名称查询公交站及其途经线路摘要；
- `v3/bus/linename`：按线路名称查询两个方向、首末班时间、完整站序和 `polyline`；请求线路时使用 `extensions=all`；
- 高德返回的 `id`、`direc`、`location`、`busstops`、`polyline` 等字段按字符串优先接收，解析后写入规范化表。

课程版定位为单体 Web 应用，数据量按数百条线路、数万站序和百万级轨迹点设计，不引入微服务、Redis、消息队列、Celery、GIS 数据库、实时车辆预测、定时任务、数据仓库或生产级多进程调度。所有历史导入和用户触发的补数都可追踪、可重试、可幂等。

## 2. 角色和功能

### 2.1 普通用户（`passenger`）

1. 注册、登录、退出、查看当前账号。
2. 输入站点名称，先查数据库；数据库没有有效结果时，调用高德站点查询并把结果增量入库后返回。
3. 查看站点坐标、数据来源、最后更新时间和途经线路；站点详情页打开时写入一次站点详情访问事件。
4. 点击线路查看该方向的起点、终点、首末班、线路类型、完整经停站顺序和线路地图。
5. 在线路地图上点击站点查看站点详情；若该站点或线路站序不在库中，先按高德线路查询补齐，再返回详情；失败时明确返回“高德未查到该数据”。
6. 收藏、取消收藏、分页查看公交站和线路收藏。
7. 地图加载失败、拒绝定位或上游不可用时，站名搜索和表格列表仍可使用。

本版本不展示“预计几分钟到站”“车辆距离”“拥挤度”“无障碍车辆”“计划发车”等上海接口专属信息。

### 2.2 分析师（`analyst`）

分析师拥有普通用户的查询能力，并可：

1. 查看站点密度热力图：统计空间网格内的有效物理站点数。
2. 查看线路密度热力图：统计空间网格内穿过的不同方向线路数；同一线路多个轨迹点只计一次。
3. 按时间范围查看普通用户站点详情访问次数排序。指标名称必须是“站点详情访问次数”，不是客流量。
4. 选择一个站点查看访问时间分布，默认按上海本地时间 0-23 时聚合，并可切换按日期、星期几；只统计 `passenger` 角色，匿名访问单独标为 `anonymous`，不混入普通用户排名。
5. 查看导入运行记录、数据源、请求关键词、成功/失败计数和错误摘要。

第一版不实现 CSV 导出、复杂预测模型、真实客流推断和自由 SQL 查询。

### 2.3 管理员（`admin`）

管理员拥有分析师全部能力，并可创建、启用、停用账号和修改角色；可逻辑停用线路、站点，查看导入记录。线路、站点和日志不提供常规物理删除。

## 3. 数据源、坐标与增量入库规则

### 3.1 高德响应到数据库的映射

| 高德字段 | 目标字段 | 规则 |
|---|---|---|
| `busstops[].id` | `bus_stops.amap_stop_id` | 字符串保存，前导零不得丢失 |
| `busstops[].name` | `bus_stops.stop_name` | 原样保存展示名，另计算规范化名称用于匹配 |
| `busstops[].location` | `bus_stops.longitude/latitude` | 解析为 GCJ-02 十进制度；非法坐标使该条导入失败 |
| `buslines[].id` | `bus_lines.amap_line_id` | 每个方向一条记录 |
| `buslines[].direc` | `bus_lines.amap_reverse_line_id` | 只作高德原始关联，不能替代内部主键 |
| `buslines[].name` | `bus_lines.amap_name`、`line_name` | `amap_name` 原样保留；`line_name` 去掉起终点括号用于搜索 |
| `type`、`citycode`、`start_stop`、`end_stop` | 同名 `amap_*` 或展示字段 | 可空，按字符串接收 |
| `start_time`、`end_time` | `first_departure_time`、`last_departure_time` | 支持 `HHmm` 和 `HH:mm`，非法值为 `NULL` 并记录警告 |
| `polyline` | `polyline_raw` 与 `bus_line_path_points` | 保留原串，同时拆为有序轨迹点 |
| `busstops[].sequence` | `bus_line_stops.sequence_no` | 从 1 开始且连续；不连续时整条线路事务回滚 |

高德站点接口中嵌套的 `buslines` 只有线路摘要，只用于发现待查询线路；完整站序和轨迹必须再次调用线路接口取得。高德返回的未知字段不写入业务表，不把 API Key 写入日志。

### 3.2 坐标约定

高德 `location` 和 `polyline` 使用 GCJ-02。表中每条站点和轨迹记录的坐标系固定为 `GCJ02`，不得假装为 WGS84。地图推荐使用 MapLibre GL JS 的 GeoJSON source/layer：

1. 后端输出 GeoJSON 前，将 GCJ-02 转换为 WGS84；转换函数放在 `app/geo/coord.py`，只允许中国大陆范围内坐标转换，越界坐标返回错误。
2. 若部署采用高德 JS 地图，则跳过转换并把坐标直接绘制；地图适配器必须明确声明 `coordinate_system=GCJ02`。
3. 底图、瓦片和转换算法必须在 README 中标注来源、许可和密钥要求；不能使用 MapLibre 的演示瓦片作为正式验收底图。

MapLibre 的 GeoJSON source 可以动态更新，内置 `line` 和 `heatmap` 图层足以绘制线路轨迹、站点和两种密度图；官方示例分别说明了 GeoJSON 线和 heatmap layer 的用法：<https://maplibre.org/maplibre-gl-js/docs/examples/>。

### 3.3 数据库优先的增量查询流程

所有站点搜索、线路详情和地图站点点击均执行同一流程：

1. 校验用户输入、城市编码和分页上限；对关键词做 Unicode 空白压缩和大小写无关的 `normalized_name` 处理。
2. 先查询 `is_active=TRUE` 的本地表，并在响应中标记 `data_source="database"`。
3. 本地无结果时，建立 `ingestion_runs(status="running", trigger_type="user_request")`，调用相应高德 API。
4. `running` 记录先用独立短事务提交；校验 HTTP 状态和高德 `status/info/infocode` 后，成功响应在另一个事务中 upsert 线路、站点、线路站序和轨迹点。
5. 提交事务后重新读取本地结果，标记 `data_source="amap"` 和 `ingestion_run_id`。
6. 最后用独立短事务把运行记录置为 `success`、`partial` 或 `failed`；业务事务回滚后仍必须保留失败运行记录。
7. 高德返回空结果时，运行记录为 `success` 且计数为 0，接口返回 `404 NOT_FOUND_AFTER_AMAP`；网络、Key 或业务错误返回 `503 AMAP_UNAVAILABLE` 或 `502 AMAP_BUSINESS_ERROR`，不写半条业务数据。
8. 站点接口只提供途经线路摘要。站点详情发现 `line_membership_status != 'complete'` 时，按摘要中的线路名称调用 `linename`，并用摘要 `id` 在响应中精确选择方向；只有所有摘要都已导入且都包含该站点时才标记 `complete`，否则标记 `partial` 并返回已确认线路和未解析摘要。
9. 上游失败不记录站点详情访问成功事件；本地详情成功返回后才写入访问事件。

同一外部 ID 优先匹配；无 ID 时按 `city_code + normalized_name + 坐标 50 米内` 匹配。候选超过一个时不自动合并，记录 `partial` 和冲突摘要。线路导入先完整解析和校验新站序/轨迹，再在同一事务中替换旧版本，禁止留下半条路线。

## 4. 技术路线

### 4.1 总体架构

```text
Vue 3 + Vite + TypeScript + Pinia + Vue Router
                    |
                REST/JSON
                    |
FastAPI + SQLAlchemy 2 同步 Session + Pydantic 2
                    |
              MySQL 8 / InnoDB
                    |
高德 Web Service (stopname / linename)
```

### 4.2 语言和框架

- 后端语言：Python 3.12；使用 FastAPI、SQLAlchemy 2、PyMySQL、Alembic、Pydantic 2、HTTPX。
- 前端语言：TypeScript；使用 Vue 3、Vite、Pinia、Vue Router、`maplibre-gl`。
- 数据库建库语言：MySQL 8 SQL。所有正式 DDL 由 Alembic 迁移生成并人工校对；不得用 `Base.metadata.create_all()` 代替迁移。
- 鉴权：`pwdlib[argon2]` 哈希密码，PyJWT Bearer Token；角色权限在后端依赖中校验。
- 测试与质量：pytest、pytest-cov、Ruff、mypy；HTTPX mock 只测本地假响应，不在单元测试访问高德。
- 时间：业务时间统一解释为 `Asia/Shanghai`，写入 `DATETIME(3)` 的上海本地时间。

### 4.3 后端目录边界

```text
backend/
  app/api/                 # REST 路由、权限依赖
  app/core/                # 配置、安全、时间、日志
  app/db/                  # Base、Session、事务
  app/models/              # 本规划的 9 张表
  app/schemas/             # API 请求/响应和高德 DTO
  app/services/            # 数据库优先查询、upsert、统计
  app/integrations/amap/   # 高德 Client、Schema、Parser
  app/geo/                 # GCJ-02/WGS84 转换、GeoJSON 构造
  app/commands/            # 样例导入和创建管理员
  alembic/                 # 唯一首次迁移及后续迁移
  tests/                   # 单元、集成、权限、契约测试
```

不创建 `integrations/shanghai`、`arrival_infos`、`dispatch_*`、`jobs/scheduler.py`、`Vehicle` 或 Repository 层。

## 5. 数据库最终设计

### 5.1 通用规则

1. 表和字段使用 `snake_case`；内部主键为 `BIGINT UNSIGNED AUTO_INCREMENT`。
2. 所有外部 ID 使用 `VARCHAR`，允许 `NULL`；外部 ID 不是主键。
3. 字符集 `utf8mb4`，排序规则 `utf8mb4_0900_ai_ci`，引擎 `InnoDB`。
4. 时间使用 `DATETIME(3)`；坐标使用 `DECIMAL(10,7)`。
5. 基础表用 `is_active` 逻辑停用；历史日志和导入记录只追加。
6. 线路方向是一条独立 `bus_lines` 记录；不能用线路名称作为唯一线路。

### 5.2 `users`

| 字段 | 类型 | 空值 | 说明 |
|---|---|---:|---|
| `id` | BIGINT UNSIGNED | 否 | 主键 |
| `username` | VARCHAR(64) | 否 | 唯一登录名 |
| `password_hash` | VARCHAR(255) | 否 | Argon2 哈希 |
| `role` | ENUM('passenger','analyst','admin') | 否 | 默认 `passenger` |
| `is_active` | BOOLEAN | 否 | 默认 TRUE |
| `created_at` / `updated_at` | DATETIME(3) | 否 | 审计时间 |

`UNIQUE(username)`；索引 `(role,is_active)` 仅在管理员筛选实际需要时建立。

### 5.3 `bus_stops`

| 字段 | 类型 | 空值 | 说明 |
|---|---|---:|---|
| `id` | BIGINT UNSIGNED | 否 | 内部站点主键 |
| `amap_stop_id` | VARCHAR(64) | 是 | 高德 `busstops[].id`，唯一 |
| `stop_name` | VARCHAR(150) | 否 | 展示站名 |
| `normalized_name` | VARCHAR(150) | 否 | 去空白后的匹配键 |
| `longitude` / `latitude` | DECIMAL(10,7) | 否 | GCJ-02 |
| `coordinate_system` | ENUM('GCJ02') | 否 | 明确坐标系 |
| `city_code` | VARCHAR(16) | 是 | 高德 `citycode` |
| `line_membership_status` | ENUM('unknown','partial','complete') | 否 | 途经线路是否已由线路接口完整确认 |
| `lines_checked_at` | DATETIME(3) | 是 | 最近一次核对线路摘要的时间 |
| `last_ingestion_run_id` | BIGINT UNSIGNED | 是 | 外键到最近修改该站点的 `ingestion_runs.id` |
| `is_active` | BOOLEAN | 否 | 逻辑状态 |
| `created_at` / `updated_at` | DATETIME(3) | 否 | 审计时间 |

约束：`UNIQUE(amap_stop_id)`；`line_membership_status` 默认 `unknown` 且只能取三种枚举值；`last_ingestion_run_id` 外键到 `ingestion_runs.id`，使用 `ON DELETE RESTRICT`。索引 `(normalized_name,is_active)`、`(city_code,normalized_name,is_active)`、`(longitude,latitude)`；经纬度范围 `CHECK`。

### 5.4 `bus_lines`

每个方向一行，字段命名保留高德语义：

| 字段 | 类型 | 空值 | 说明 |
|---|---|---:|---|
| `id` | BIGINT UNSIGNED | 否 | 内部方向线路主键 |
| `amap_line_id` | VARCHAR(64) | 否 | 高德 `buslines[].id`，唯一 |
| `amap_reverse_line_id` | VARCHAR(64) | 是 | 高德 `direc` |
| `line_name` | VARCHAR(100) | 否 | 去掉括号的搜索名，如 `980路` |
| `amap_name` | VARCHAR(255) | 否 | 高德完整名称 |
| `amap_type` | VARCHAR(32) | 是 | 高德 `type` |
| `city_code` | VARCHAR(16) | 是 | 高德 `citycode` |
| `start_stop_name` / `end_stop_name` | VARCHAR(150) | 是 | 高德起终点展示值 |
| `first_departure_time` / `last_departure_time` | TIME | 是 | 高德 `start_time/end_time` |
| `loop_flag` | BOOLEAN | 是 | 高德 `loop` 转换值 |
| `amap_status` | VARCHAR(16) | 是 | 高德 `status` |
| `company_name` | VARCHAR(150) | 是 | 高德 `company` |
| `distance_km` | DECIMAL(10,3) | 是 | 高德 `distance` |
| `basic_price` / `total_price` | DECIMAL(8,2) | 是 | 高德票价 |
| `bounds_raw` | VARCHAR(255) | 是 | 高德 `bounds` 原串 |
| `polyline_raw` | MEDIUMTEXT | 否 | 高德原始轨迹串 |
| `last_ingestion_run_id` | BIGINT UNSIGNED | 是 | 外键到最近修改该线路的 `ingestion_runs.id` |
| `is_active` | BOOLEAN | 否 | 逻辑状态 |
| `created_at` / `updated_at` | DATETIME(3) | 否 | 审计时间 |

约束：`UNIQUE(amap_line_id)`；`last_ingestion_run_id` 外键到 `ingestion_runs.id`，使用 `ON DELETE RESTRICT`；索引 `(line_name,is_active)`、`(city_code,line_name,is_active)`；同名不同方向不得合并。

### 5.5 `bus_line_stops`

| 字段 | 类型 | 空值 | 说明 |
|---|---|---:|---|
| `id` | BIGINT UNSIGNED | 否 | 主键 |
| `line_id` | BIGINT UNSIGNED | 否 | 外键 `bus_lines.id` |
| `stop_id` | BIGINT UNSIGNED | 否 | 外键 `bus_stops.id` |
| `sequence_no` | SMALLINT UNSIGNED | 否 | 高德 `sequence`，从 1 连续 |
| `amap_stop_id_snapshot` | VARCHAR(64) | 是 | 导入时的高德站点 ID，便于审计 |
| `ingestion_run_id` | BIGINT UNSIGNED | 否 | 生成当前站序版本的导入运行 |

`UNIQUE(line_id,sequence_no)`；`ingestion_run_id` 外键到 `ingestion_runs.id`，使用 `ON DELETE RESTRICT`；普通索引 `(stop_id)`、`(line_id,stop_id)`。不建立 `(line_id,stop_id)` 唯一约束，以兼容环线重复经过同一站。

### 5.6 `bus_line_path_points`

| 字段 | 类型 | 空值 | 说明 |
|---|---|---:|---|
| `id` | BIGINT UNSIGNED | 否 | 主键 |
| `line_id` | BIGINT UNSIGNED | 否 | 外键 `bus_lines.id` |
| `sequence_no` | INT UNSIGNED | 否 | `polyline` 点顺序，从 1 连续 |
| `longitude` / `latitude` | DECIMAL(10,7) | 否 | GCJ-02 |
| `coordinate_system` | ENUM('GCJ02') | 否 | 坐标系 |
| `ingestion_run_id` | BIGINT UNSIGNED | 否 | 生成当前轨迹版本的导入运行 |

`UNIQUE(line_id,sequence_no)`；`ingestion_run_id` 外键到 `ingestion_runs.id`，使用 `ON DELETE RESTRICT`；索引 `(line_id,sequence_no)`。导入时先删除该线路旧点，再批量插入新点，和线路主记录、站序在同一事务内提交。

### 5.7 `favorite_stops` 与 `favorite_lines`

两表结构相同：`user_id BIGINT UNSIGNED`、`stop_id` 或 `line_id BIGINT UNSIGNED`、`created_at DATETIME(3)`。联合主键为 `(user_id,stop_id)` 或 `(user_id,line_id)`；用户删除 `ON DELETE CASCADE`，基础对象删除 `RESTRICT`；分别建立反向外键索引。添加收藏必须幂等。

### 5.8 `stop_view_events`

用于分析普通用户打开站点详情的行为，不记录地图移动、自动补全和失败请求。

| 字段 | 类型 | 空值 | 说明 |
|---|---|---:|---|
| `id` | BIGINT UNSIGNED | 否 | 主键 |
| `user_id` | BIGINT UNSIGNED | 是 | 未登录为 NULL，删除用户后 SET NULL |
| `actor_role` | ENUM('anonymous','passenger','analyst','admin') | 否 | 写入事件发生时的角色快照 |
| `stop_id` | BIGINT UNSIGNED | 否 | 被打开的站点 |
| `entry_point` | ENUM('search','line_map','favorite','direct') | 否 | 进入详情的入口 |
| `viewed_at` | DATETIME(3) | 否 | 上海本地时间 |

索引：`(stop_id,viewed_at)`、`(actor_role,viewed_at)`、`(viewed_at)`。排名统计条件为 `actor_role='passenger'`；时间分布接口可选择 `passenger` 或 `all`。

外键：`user_id -> users.id ON DELETE SET NULL`，`stop_id -> bus_stops.id ON DELETE RESTRICT`；两列均使用 `ON UPDATE RESTRICT`。

### 5.9 `ingestion_runs`

| 字段 | 类型 | 空值 | 说明 |
|---|---|---:|---|
| `id` | BIGINT UNSIGNED | 否 | 主键 |
| `endpoint` | ENUM('stopname','linename') | 否 | 调用的高德接口 |
| `trigger_type` | ENUM('sample_import','manual','user_request') | 否 | 触发方式 |
| `request_keyword` | VARCHAR(255) | 是 | 脱敏后的关键词 |
| `city_code` | VARCHAR(16) | 是 | 城市范围 |
| `started_at` | DATETIME(3) | 否 | 运行开始时间 |
| `finished_at` | DATETIME(3) | 是 | 运行结束时间 |
| `status` | ENUM('running','success','partial','failed') | 否 | 运行状态 |
| `received_count` / `inserted_count` / `updated_count` / `skipped_count` / `failed_count` | INT UNSIGNED | 否 | 默认 0 |
| `error_message` | TEXT | 是 | 截断错误摘要，不含 Key |

索引 `(status,started_at)`、`(endpoint,started_at)`、`(started_at)`。一次响应只能对应一个运行记录；重试生成新记录，不覆盖历史。

### 5.10 关系总览

```text
users 1--N stop_view_events N--1 bus_stops
users M--N bus_stops  via favorite_stops
users M--N bus_lines  via favorite_lines
bus_lines 1--N bus_line_stops N--1 bus_stops
bus_lines 1--N bus_line_path_points
ingestion_runs 1--N bus_stops / bus_lines / bus_line_stops / bus_line_path_points
```

正式数据库共 9 张表：`users`、`bus_stops`、`bus_lines`、`bus_line_stops`、`bus_line_path_points`、`favorite_stops`、`favorite_lines`、`stop_view_events`、`ingestion_runs`。

## 6. REST API 契约

所有列表接口限制 `limit<=100`，输入使用 Pydantic 校验，Service 使用 SQLAlchemy 参数绑定。

```text
POST   /api/auth/register
POST   /api/auth/login
GET    /api/auth/me

GET    /api/stops/search?q=&city_code=&limit=
GET    /api/stops/{stop_id}
GET    /api/stops/{stop_id}/lines
GET    /api/lines/{line_id}
GET    /api/lines/{line_id}/stops
GET    /api/lines/{line_id}/map                 # GeoJSON LineString + 站点
GET    /api/lines/by-amap/{amap_line_id}?line_name=&city_code= # 未解析摘要点击时按高德 ID 补齐

GET    /api/me/favorite-stops
PUT    /api/me/favorite-stops/{stop_id}
DELETE /api/me/favorite-stops/{stop_id}
GET    /api/me/favorite-lines
PUT    /api/me/favorite-lines/{line_id}
DELETE /api/me/favorite-lines/{line_id}

GET    /api/analytics/heatmaps/stops?bbox=&grid_size_m=
GET    /api/analytics/heatmaps/lines?bbox=&grid_size_m=
GET    /api/analytics/stops/popularity?start_at=&end_at=&limit=
GET    /api/analytics/stops/{stop_id}/view-distribution?start_at=&end_at=&bucket=hour

GET    /api/admin/users
POST   /api/admin/users
PATCH  /api/admin/users/{user_id}
GET    /api/admin/ingestion-runs
GET    /api/admin/ingestion-runs/{run_id}
PATCH  /api/admin/stops/{stop_id}/status
PATCH  /api/admin/lines/{line_id}/status
```

### 6.1 统一响应和错误

成功响应包含 `data_source: "database" | "amap"`；上游补入时包含 `ingestion_run_id`。本地无结果且高德空结果为 `404`、错误码 `NOT_FOUND_AFTER_AMAP`；Key/网络/超时为 `503 AMAP_UNAVAILABLE`；高德业务失败为 `502 AMAP_BUSINESS_ERROR`。前端不得把上游失败伪装成空列表。

### 6.2 热力图计算定义

请求 `bbox=min_lon,min_lat,max_lon,max_lat` 和 `grid_size_m`（默认 300，允许 100-2000）。后端先按坐标范围筛选，使用等距投影的网格键聚合：

- 站点热力图：每格 `weight = COUNT(DISTINCT stop_id)`；
- 线路热力图：将每条 `polyline` 线段按约 100 米采样，按 `(grid_key,line_id)` 去重后 `weight = COUNT(DISTINCT line_id)`；
- 输出 GeoJSON `FeatureCollection`，每个 Feature 为网格中心 Point，属性包含 `weight`、`grid_size_m`、`metric`；
- 无有效轨迹点时返回空 FeatureCollection，不调用高德地图服务。

## 7. 前端和地图交互建议

### 7.1 普通用户页面

首页采用“站名搜索 + 结果列表 + 右侧地图”布局；点击站点进入详情，详情列出途经线路；点击线路进入线路详情，左侧顺序站点列表、右侧线路轨迹，点击轨迹上的站点打开详情并记录 `entry_point=line_map`。收藏使用图标按钮并提供可访问性标签。移动端改为上下布局，列表仍可脱离地图操作。

### 7.2 分析师页面

提供“站点密度/线路密度”分段切换、时间范围、网格大小和城市范围；热力层之外保留站点/线路列表。另提供站点访问次数排行表，点击一行展示该站点按小时或日期的分布图。

### 7.3 地图技术取舍

推荐 MapLibre GL JS + 可商用的 WGS84 矢量瓦片底图：轨迹、站点、GeoJSON 热力图都在浏览器本地渲染，地图组件不承担高德数据查询。高德地图 JS API 是可选适配器，适用于不想维护 GCJ-02 转换的部署；不能把高德 Web Service 的 GCJ-02 坐标直接叠加到 OSM/WGS84 底图。地图供应商密钥、配额和许可必须单独配置，不能提交到仓库。

## 8. 开发流程（与 `backend/开发顺序.md` 同步）

### 阶段 0：项目初始化与配置

建立 Python 3.12 虚拟环境、`.env.example`、Settings、上海时区工具、日志脱敏规则；删除所有上海配置项和调度配置。完成 `test_config.py`、`test_time.py`。

### 阶段 1：数据库基础设施

完成 `app/db/base.py` 的 Base、BIGINT UNSIGNED、时间 mixin 和命名约束；完成同步 Engine、Session、事务依赖。验收：测试库可以执行 `SELECT 1`，请求结束 Session 关闭。

### 阶段 2：9 张 ORM 表

按本章顺序实现 `account.py`、`transit.py`、`ingestion.py`，建立全部关系、外键、唯一约束、索引和范围检查。验收：`Base.metadata` 恰好有 9 张表，结构契约测试通过。

### 阶段 3：Alembic 首次迁移

生成单一首次迁移，先建 `ingestion_runs` 再建引用它的公交表，人工校对 MySQL 8 的 `UNSIGNED`、`DATETIME(3)`、`MEDIUMTEXT`、`ENUM`、外键删除策略和关键索引；测试空库 `upgrade -> downgrade -> upgrade`。

### 阶段 4：高德 Schema、Parser 和坐标工具

实现 `app/integrations/amap/schemas.py`、`parser.py`、`client.py` 和 `app/geo/coord.py`。覆盖站点查询、线路两个方向、字符串数字、空字段、非法坐标、polyline 拆分、站序连续性和 `direc` 互指校验。单元测试禁止联网。

### 阶段 5：高德样例导入和幂等 Upsert

实现 `services/ingestion.py` 和 `commands/import_samples.py`：先完整解析，后在单事务内 upsert 站点/线路、重建站序和轨迹点，写入 `ingestion_runs`；重复同一响应不能产生重复基础记录。集成测试覆盖回滚、冲突、两方向和大 polyline。

### 阶段 6：数据库优先查询与按需补数

实现 `services/transit.py`、`services/on_demand_sync.py` 和站点/线路路由。每个查询先读库，空结果才调用高德；站点线路关系为 `unknown/partial` 时按摘要补齐完整线路；成功补入后重新读库；空响应、Key 缺失、超时和业务错误按第 6.1 节返回。测试本地完整命中不发 HTTP、未命中按实际高德请求次数分别记录运行、上游失败无半条数据。

### 阶段 7：认证与收藏

完成 Argon2、JWT、注册/登录/current-user、Passenger/Analyst/Admin 权限依赖；实现双收藏接口和幂等写入。测试密码哈希不回显、停用用户不能登录、越权返回 403、重复收藏不重复。

### 阶段 8：站点详情访问日志

在站点详情成功返回的 Service 内写入 `stop_view_events`，记录角色快照和入口；地图移动、搜索建议、404 和上游失败不写入。测试事务提交后有一条事件，失败回滚不留事件。

### 阶段 9：分析接口

实现网格生成、轨迹 100 米采样、线路去重、GeoJSON 响应和四个分析接口。所有分析只读数据库；排行默认过滤 `passenger`；时间分布显式处理上海时区和空区间。用固定数据人工复算站点密度、线路密度、排行和 24 小时分布。

### 阶段 10：管理员和应用装配

完成管理员账号管理、逻辑启停、导入记录查询、CORS、OpenAPI、`/health` 和统一异常处理；确认启动不自动建表、不启动 Scheduler。验收管理员拥有分析师权限但分析师不能改账号。

### 阶段 11：Vue 前端和端到端验收

完成搜索、站点详情、线路站序/地图、双收藏、热力图、排行与时间分布页面；处理加载、空数据、上游错误、地图不可用、移动端和权限状态。端到端验收覆盖“数据库命中”和“高德补入”两条路径。

## 9. 验收与人工校对清单

- [ ] 代码、配置、文档和迁移中不存在 `shanghai`、`arrival_infos`、`dispatch_schedules`、`dispatch_cars`、`scheduler` 业务依赖。
- [ ] 数据库恰好 9 张表，只有高德外部 ID，线路两个方向分开。
- [ ] `polyline_raw` 与有序 `bus_line_path_points` 同时存在且可重建线路 GeoJSON。
- [ ] 所有高德坐标标注 GCJ-02；MapLibre/WGS84 渲染前有转换或明确使用高德底图。
- [ ] 站点搜索和线路地图点击严格数据库优先，补数成功后再返回数据库结果。
- [ ] 高德空结果和高德故障错误码不同；失败不写半条业务数据。
- [ ] 普通用户站点详情访问次数排行不称为客流量；时间分布默认 24 个上海本地小时桶。
- [ ] 收藏站点和线路均可增删查，重复操作幂等。
- [ ] 分析师不可管理用户，管理员同时具备分析和账号管理能力。
- [ ] 所有查询使用参数绑定，API Key/JWT 密钥不进入日志、响应或 Git。
- [ ] `ruff check`、`mypy`、`pytest`、Alembic 升降级和端到端演示全部通过。

## 10. 明确不实施

不实施上海 API 的任何字段或客户端；不实施实时车辆、到站预测、调度计划、自动定时抓取、缓存、重试队列、GIS 扩展、真实客流推断、CSV 导出、复杂推荐和全上海完整数据镜像。后续扩展必须新增 Alembic 迁移，不修改已执行的历史迁移。

## 11. Python 虚拟环境与 requirements

推荐环境命令：

```bash
cd backend
python3.12 -m venv .venv
source .venv/bin/activate
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
```

`backend/requirements.txt` 内容如下；版本使用上下界，实际锁定版本应在新项目初始化时生成 `requirements.lock`：

```text
# runtime
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pydantic>=2.10,<3
pydantic-settings>=2.6,<3
SQLAlchemy>=2.0.36,<2.1
PyMySQL>=1.1,<2
alembic>=1.14,<2
httpx>=0.28,<1
PyJWT>=2.10,<3
pwdlib[argon2]>=0.2,<1
tzdata>=2024.2
python-multipart>=0.0.20,<1

# development and test
pytest>=8.3,<9
pytest-cov>=6,<7
ruff>=0.9,<1
mypy>=1.14,<2
types-PyMySQL>=1.1,<2
```

不安装 APScheduler、上海 API SDK、GeoPandas、Shapely、GeoAlchemy 或 Redis 客户端；空间网格和坐标转换由本项目的轻量 Python 模块完成。
