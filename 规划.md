# 城市公共交通实时状态查询与分析系统实施规划

## 1. 文档目标与实施边界

本文档依据以下材料确定项目的最终实施方案：

- `2452443_龙羿_数据库系统设计-2.pdf` 中的需求分析、E-R 设计、逻辑设计、索引设计和更新策略；
- `对话疑问回答.md` 中对数据库调整方案的最终裁决；
- 上海市政府接口样例 `raw_result.json`；
- 高德站点查询样例 `bus_stop_by_name.json`；
- 高德线路查询样例 `bus_line_raw_gaode.json`。

本文档可直接交给 AI coder 分阶段实施，也可作为人工校对、验收和后续报告修订的依据。实施时应以本文档中的最终数据库结构为准；原报告中的表结构作为设计来源，不再机械照搬。

项目定位为课程实验规模的单体 Web 应用。预计管理的站点不超过 80 个，不追求全上海公交数据的完整覆盖，不引入微服务、消息队列、Redis、GIS 扩展或数据仓库。优先保证数据库关系正确、采集过程可追踪、主要查询可用、代码容易说明和演示。

## 2. 功能清单

### 2.1 普通乘客功能

1. 注册、登录、退出和获取当前用户信息。
2. 根据浏览器提供的经纬度查询附近公交站点。
3. 按站点名称搜索站点。
4. 查看某站点经过的线路及其方向。
5. 查看某线路在该站点最近两辆车的预测到站时间、距离、剩余站数、拥挤程度和无障碍状态。
6. 查看线路的完整站点顺序、起点、终点、首末班时间和可换乘线路。
7. 查看首站近期计划发车信息。
8. 收藏、取消收藏和查看收藏站点。
9. 用户选择站点并查询时记录查询日志。
10. 手动刷新实时信息；后端应先执行短时缓存判断，避免连续刷新直接重复调用上游接口。

### 2.2 交通分析师功能

1. 按线路、站点和时间范围查看预测候车时间趋势。
2. 根据调度快照查看计划发车间隔及其稳定性。
3. 统计无障碍车辆在到站预测快照中的观测比例。
4. 统计各站点的系统内查询热度。
5. 导出或分页查看上述统计结果。

分析指标必须使用准确名称，不将查询次数表述为真实客流，不将预测时间表述为实际候车时间，不将计划发车表述为实际准点率。

### 2.3 系统管理员功能

1. 创建、启用、停用用户并分配乘客、分析师、管理员角色。
2. 查看线路、站点及双 API 标识的匹配结果。
3. 启用或停用线路、站点，不对基础数据执行物理删除。
4. 手动触发高德静态数据同步或市政府实时数据采集。
5. 查看每次采集任务的开始时间、结束时间、成功/失败状态、处理数量和错误信息。
6. 查看缺失字段、无法匹配线路/站点、上游接口错误等数据质量问题。
7. 定期清理或归档过期的实时快照、调度快照和查询日志。

### 2.4 数据采集功能

1. 从高德线路接口导入两个方向的线路、站点及站点顺序。
2. 从高德站点接口按站名或高德 ID 补充站点及经过线路信息。
3. 从市政府附近站点接口导入给定坐标附近返回的站点、线路、到站预测和发车计划。
4. 使用内部自增 ID 连接业务数据；高德 ID 和市政府 ID 均允许为空。市政府样例中的 `stopId` 随线路变化，不作为物理站点的全局 ID。
5. 市政府线路以 `(lineId, upDown)` 识别一个方向；高德返回的每个方向有独立线路 ID。
6. 对一次上游响应使用统一的采集时间，在事务中完成解析和入库。
7. 对缺失的 `sai`、`dispatchCars`、车辆、下一辆车等字段正常写入 `NULL`，不得因可选字段缺失使整个任务失败。

## 3. 技术选型

### 3.1 总体架构

采用前后端分离的单体架构：

```text
Vue 3 + Vite
       |
   REST/JSON
       |
FastAPI + SQLAlchemy + APScheduler
       |
    MySQL 8
       |
上海市政府 API / 高德 API
```

### 3.2 数据库

- MySQL 8.0，字符集统一使用 `utf8mb4`，排序规则使用 `utf8mb4_0900_ai_ci`。
- InnoDB 存储引擎，以支持事务、行锁和外键。
- 数据库时间统一按 `Asia/Shanghai` 解释；应用层使用带时区的 Python `datetime`，写入 MySQL 前转换为上海本地时间。项目不得混用 UTC 和本地时间。
- 使用 Alembic 管理表结构迁移，不依赖应用启动时自动建表作为正式方案。

### 3.3 后端

选择 Python 3.12、FastAPI、SQLAlchemy 2、Alembic、Pydantic 2、HTTPX 和 APScheduler。

选择 FastAPI 而不是 Django，原因如下：

- 本项目核心是 REST API、外部 API 采集与数据库查询，不需要 Django 模板系统；
- FastAPI 自动生成 OpenAPI 文档，便于人工和 AI coder 调试；
- Pydantic 适合校验两个上游接口中大量可空、类型不稳定的字段；
- SQLAlchemy 和 Alembic 能完整表达 MySQL 外键、索引和迁移；
- APScheduler 足以承担单机课程项目的 20 分钟采集任务，无需引入 Celery 和消息队列。

密码使用 `argon2` 或 `bcrypt` 哈希。认证使用短期 JWT Bearer Token。角色权限在后端依赖项中统一校验，不能只在前端隐藏按钮。

### 3.4 前端

- Vue 3、Vite、TypeScript；
- Vue Router 管理乘客、分析师和管理员页面；
- Pinia 管理登录状态和少量跨页面状态；
- Axios 或原生 `fetch` 封装统一 API 客户端；
- 地图展示可使用高德 JS API；若课程环境无法稳定加载地图，必须保留站点列表作为可用的降级界面。

## 4. 数据库最终结构

### 4.1 命名与通用约定

1. 表名和字段名使用 `snake_case`，避免 `User`、`Line` 等潜在关键字和大小写差异。
2. 内部主键使用 `BIGINT UNSIGNED AUTO_INCREMENT`，不得把外部 API ID 作为主键。
3. 外部 API ID 使用 `VARCHAR`，因为样例中存在前导零和字母，例如 `097300`、`687C0003`、`BV10030918`。
4. API 可选字段默认允许 `NULL`；布尔值未知时使用 `NULL`，不能用 `FALSE` 代替未知。
5. 基础表使用 `is_active` 实现逻辑停用。
6. 所有 `created_at`、`updated_at`、`collected_at` 使用 `DATETIME(3)`。
7. 经度、纬度使用 `DECIMAL(10,7)`，足以覆盖样例精度。
8. 外键列均建立索引。MySQL 可能为外键自动创建索引，但迁移文件仍应显式声明关键业务索引，保证结构清晰。

### 4.2 `users` 用户表

| 字段 | 类型 | 空值 | 说明 |
|---|---|---:|---|
| `id` | BIGINT UNSIGNED | 否 | 主键，自增 |
| `username` | VARCHAR(64) | 否 | 登录名 |
| `password_hash` | VARCHAR(255) | 否 | 密码哈希，不保存明文 |
| `role` | ENUM('passenger','analyst','admin') | 否 | 用户角色 |
| `is_active` | BOOLEAN | 否 | 默认 TRUE |
| `created_at` | DATETIME(3) | 否 | 创建时间 |
| `updated_at` | DATETIME(3) | 否 | 更新时间 |

约束与索引：

- `PRIMARY KEY (id)`；
- `UNIQUE KEY uq_users_username (username)`；
- 不为低基数的 `role` 单独建立索引；管理员列表同时按角色和启用状态过滤时再根据 `EXPLAIN` 决定是否增加 `(role, is_active)`。

### 4.3 `lines` 线路方向表

一条记录表示一条确定方向的线路，而不是不分方向的线路名称。

| 字段 | 类型 | 空值 | 说明 |
|---|---|---:|---|
| `id` | BIGINT UNSIGNED | 否 | 内部线路主键 |
| `line_name` | VARCHAR(100) | 否 | 展示名称，如 `980路` |
| `direction` | TINYINT UNSIGNED | 否 | 0/1；内部正式方向属性 |
| `line_type` | TINYINT UNSIGNED | 是 | 1 公交、2 地铁、3 轮渡 |
| `shanghai_line_id` | VARCHAR(32) | 是 | 市政府线路 ID |
| `amap_line_id` | VARCHAR(32) | 是 | 高德某一方向的线路 ID |
| `first_departure_time` | TIME | 是 | 首班时间 |
| `last_departure_time` | TIME | 是 | 末班时间 |
| `is_active` | BOOLEAN | 否 | 默认 TRUE，逻辑停用 |
| `created_at` | DATETIME(3) | 否 | 创建时间 |
| `updated_at` | DATETIME(3) | 否 | 更新时间 |

约束与索引：

- `PRIMARY KEY (id)`；
- `UNIQUE KEY uq_lines_amap_id (amap_line_id)`；MySQL 唯一索引允许多个 `NULL`；
- `UNIQUE KEY uq_lines_shanghai_direction (shanghai_line_id, direction)`；
- `KEY idx_lines_name_active (line_name, is_active)`；
- `CHECK (direction IN (0,1))`；
- `CHECK (line_type IS NULL OR line_type IN (1,2,3))`。

起点和终点不再保存名称字段，分别由 `line_routes` 中最小和最大 `sequence_no` 对应的站点推导。市政府样例中的 `startStopName`、`endStopName` 可用于导入匹配和异常日志，但不作为连接依据。

### 4.4 `stops` 站点表

| 字段 | 类型 | 空值 | 说明 |
|---|---|---:|---|
| `id` | BIGINT UNSIGNED | 否 | 内部站点主键 |
| `stop_name` | VARCHAR(150) | 否 | 站点名称 |
| `amap_stop_id` | VARCHAR(32) | 是 | 高德站点 ID |
| `longitude` | DECIMAL(10,7) | 否 | 经度 |
| `latitude` | DECIMAL(10,7) | 否 | 纬度 |
| `is_active` | BOOLEAN | 否 | 默认 TRUE，逻辑停用 |
| `created_at` | DATETIME(3) | 否 | 创建时间 |
| `updated_at` | DATETIME(3) | 否 | 更新时间 |

约束与索引：

- `PRIMARY KEY (id)`；
- `UNIQUE KEY uq_stops_amap_id (amap_stop_id)`；
- `KEY idx_stops_location (longitude, latitude)`，用于课程规模下的经纬度范围框预筛选；
- `KEY idx_stops_name_active (stop_name, is_active)`，支持精确和前缀查询；
- `CHECK (longitude BETWEEN -180 AND 180)`；
- `CHECK (latitude BETWEEN -90 AND 90)`。

两个数据源的坐标可能有小偏差。高德站点优先使用已有 `amap_stop_id`，其他情况使用规范化名称加距离阈值识别物理站；模糊匹配结果不得静默覆盖已确认映射。市政府样例中，同一个物理站在不同线路下返回不同 `stopId`，因此该字段不能放在 `stops` 中作为全局站点标识。

### 4.5 `line_routes` 线路站点顺序表

| 字段 | 类型 | 空值 | 说明 |
|---|---|---:|---|
| `id` | BIGINT UNSIGNED | 否 | 主键，自增 |
| `line_id` | BIGINT UNSIGNED | 否 | 外键到 `lines.id` |
| `stop_id` | BIGINT UNSIGNED | 否 | 外键到 `stops.id` |
| `sequence_no` | SMALLINT UNSIGNED | 否 | 站点顺序，从 1 开始 |
| `shanghai_stop_id` | VARCHAR(32) | 是 | 市政府接口中该线路方向对应的站点标识 |

约束与索引：

- `PRIMARY KEY (id)`；
- `CONSTRAINT fk_line_routes_line FOREIGN KEY (line_id) REFERENCES lines(id) ON UPDATE RESTRICT ON DELETE RESTRICT`；
- `CONSTRAINT fk_line_routes_stop FOREIGN KEY (stop_id) REFERENCES stops(id) ON UPDATE RESTRICT ON DELETE RESTRICT`；
- `UNIQUE KEY uq_line_routes_line_sequence (line_id, sequence_no)`；
- `UNIQUE KEY uq_line_routes_shanghai_stop (line_id, shanghai_stop_id)`；允许多个 `NULL`；
- `KEY idx_line_routes_stop (stop_id)`；
- `KEY idx_line_routes_line_stop (line_id, stop_id)`，用于线路与物理站点匹配；
- `(line_id, stop_id)` 只建立普通索引，不建立唯一约束，以兼容环线或同一线路重复经过同一站点的情况；
- `CHECK (sequence_no >= 1)`。

### 4.6 `favorite_stops` 收藏站点表

| 字段 | 类型 | 空值 | 说明 |
|---|---|---:|---|
| `user_id` | BIGINT UNSIGNED | 否 | 外键到 `users.id` |
| `stop_id` | BIGINT UNSIGNED | 否 | 外键到 `stops.id` |
| `created_at` | DATETIME(3) | 否 | 收藏时间 |

约束与索引：

- `PRIMARY KEY (user_id, stop_id)`；
- 用户删除时收藏无保留价值，使用 `ON DELETE CASCADE`；
- 站点不物理删除，站点外键使用 `ON DELETE RESTRICT`；
- `KEY idx_favorite_stops_stop (stop_id)`，用于反向统计收藏数量。

### 4.7 `query_logs` 查询日志表

| 字段 | 类型 | 空值 | 说明 |
|---|---|---:|---|
| `id` | BIGINT UNSIGNED | 否 | 主键，自增 |
| `user_id` | BIGINT UNSIGNED | 是 | 查询用户；匿名或用户删除后可为空 |
| `stop_id` | BIGINT UNSIGNED | 否 | 被查询站点 |
| `queried_at` | DATETIME(3) | 否 | 查询时间 |

约束与索引：

- `PRIMARY KEY (id)`；
- `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE SET NULL`；
- `FOREIGN KEY (stop_id) REFERENCES stops(id) ON DELETE RESTRICT`；
- `KEY idx_query_logs_stop_time (stop_id, queried_at)`，用于站点热度统计；
- `KEY idx_query_logs_user_time (user_id, queried_at)`，用于用户历史分页；
- `KEY idx_query_logs_time (queried_at)`，用于按时间清理和全局趋势统计。

只有用户真正进入站点详情或请求站点实时信息时记录日志；地图移动、自动补全和后台采集不记录，防止污染热度指标。

### 4.8 `ingestion_runs` 数据采集运行表

| 字段 | 类型 | 空值 | 说明 |
|---|---|---:|---|
| `id` | BIGINT UNSIGNED | 否 | 主键，自增 |
| `source` | ENUM('shanghai','amap') | 否 | 数据来源 |
| `task_type` | VARCHAR(50) | 否 | `nearby_realtime`、`line_sync`、`stop_sync` 等 |
| `trigger_type` | ENUM('scheduled','manual','user_request') | 否 | 触发方式 |
| `request_key` | VARCHAR(255) | 是 | 坐标、线路 ID 或站名等脱敏后的请求标识 |
| `started_at` | DATETIME(3) | 否 | 开始时间 |
| `finished_at` | DATETIME(3) | 是 | 完成时间 |
| `status` | ENUM('running','success','partial','failed') | 否 | 运行状态 |
| `received_count` | INT UNSIGNED | 否 | 默认 0 |
| `inserted_count` | INT UNSIGNED | 否 | 默认 0 |
| `updated_count` | INT UNSIGNED | 否 | 默认 0 |
| `skipped_count` | INT UNSIGNED | 否 | 默认 0 |
| `failed_count` | INT UNSIGNED | 否 | 默认 0 |
| `error_message` | TEXT | 是 | 截断后的错误摘要，不保存密钥 |

约束与索引：

- `PRIMARY KEY (id)`；
- `KEY idx_ingestion_runs_status_started (status, started_at)`；
- `KEY idx_ingestion_runs_source_task_started (source, task_type, started_at)`；
- `KEY idx_ingestion_runs_started (started_at)`，用于归档清理。

采集任务先提交一条 `running` 记录，再调用上游 API。任务结束后更新统计和最终状态。即使业务数据事务回滚，也必须保留失败运行记录，因此运行记录与业务数据使用分开的事务边界。

### 4.9 `arrival_infos` 实时到站快照表

按照最终裁决，保留 API 固定返回最近两辆车的横向结构，不再建立 `vehicles` 表。

| 字段 | 类型 | 空值 | 说明 |
|---|---|---:|---|
| `id` | BIGINT UNSIGNED | 否 | 主键，自增 |
| `ingestion_run_id` | BIGINT UNSIGNED | 否 | 对应采集运行 |
| `line_id` | BIGINT UNSIGNED | 否 | 内部线路 |
| `stop_id` | BIGINT UNSIGNED | 否 | 内部站点 |
| `source_up_down` | TINYINT UNSIGNED | 是 | 市政府 `sai.upDown` 原始兼容值，不参与业务判向 |
| `collected_at` | DATETIME(3) | 否 | 本次采集统一时间 |
| `current_bus_distance_m` | INT UNSIGNED | 是 | 最近车辆距离，米 |
| `current_bus_arrival_min` | INT UNSIGNED | 是 | 预计到站分钟数 |
| `current_bus_comfort` | TINYINT UNSIGNED | 是 | 0 未知、1 舒适、2 较舒适、3 拥挤 |
| `current_bus_stop_count` | SMALLINT UNSIGNED | 是 | 剩余站数 |
| `current_license_plate` | VARCHAR(64) | 是 | API 原始车辆文本 |
| `current_barrier_free` | BOOLEAN | 是 | 本次快照观测值 |
| `next_bus_distance_m` | INT UNSIGNED | 是 | 下一辆车距离，米 |
| `next_bus_arrival_min` | INT UNSIGNED | 是 | 下一辆车预计到站分钟数 |
| `next_bus_stop_count` | SMALLINT UNSIGNED | 是 | 下一辆车剩余站数 |
| `next_license_plate` | VARCHAR(64) | 是 | API 原始车辆文本 |
| `next_barrier_free` | BOOLEAN | 是 | 本次快照观测值 |

约束与索引：

- `PRIMARY KEY (id)`；
- `FOREIGN KEY (ingestion_run_id) REFERENCES ingestion_runs(id) ON DELETE RESTRICT`；
- `FOREIGN KEY (line_id) REFERENCES lines(id) ON DELETE RESTRICT`；
- `FOREIGN KEY (stop_id) REFERENCES stops(id) ON DELETE RESTRICT`；
- `UNIQUE KEY uq_arrival_run_line_stop (ingestion_run_id, line_id, stop_id)`，防止同一采集批次的同一线路站点重复写入；
- `KEY idx_arrival_realtime (line_id, stop_id, collected_at)`，用于指定线路站点的最新快照查询；
- `KEY idx_arrival_stop_time (stop_id, collected_at DESC)`；
- `KEY idx_arrival_line_time (line_id, collected_at)`，用于不限定站点的线路历史分析；
- `KEY idx_arrival_time (collected_at)`，用于时间范围分析和清理；
- 最新到站查询使用 `idx_arrival_realtime`，按固定线路、站点反向扫描采集时间；
- 对距离、分钟数、剩余站数和拥挤度增加非负值及枚举范围 `CHECK`。

车牌字段保留上游原始字符串，例如样例中的“沪A06328D无障碍”。无障碍分析以明确的布尔字段为准，不依赖车牌文本后缀推断；仅当布尔字段缺失时可在解析层尝试从文本补充，并记录为清洗规则。

### 4.10 `dispatch_schedules` 调度快照表

| 字段 | 类型 | 空值 | 说明 |
|---|---|---:|---|
| `id` | BIGINT UNSIGNED | 否 | 主键，自增 |
| `ingestion_run_id` | BIGINT UNSIGNED | 否 | 对应采集运行 |
| `line_id` | BIGINT UNSIGNED | 否 | 内部线路 |
| `collected_at` | DATETIME(3) | 否 | 采集时间 |
| `schedule_code` | SMALLINT | 是 | 样例：-1 不运营、0 暂无数据、1 正常 |
| `message_default` | VARCHAR(255) | 是 | 完整提示文本 |
| `message_short` | VARCHAR(255) | 是 | 简短提示文本 |

约束与索引：

- `PRIMARY KEY (id)`；
- `FOREIGN KEY (ingestion_run_id) REFERENCES ingestion_runs(id) ON DELETE RESTRICT`；
- `FOREIGN KEY (line_id) REFERENCES lines(id) ON DELETE RESTRICT`；
- `UNIQUE KEY uq_dispatch_schedule_run_line (ingestion_run_id, line_id)`，防止同一采集批次重复保存同一线路的调度快照；
- `KEY idx_dispatch_schedule_line_time (line_id, collected_at)`，用于最新调度和线路历史查询；
- `KEY idx_dispatch_schedule_time (collected_at)`；
- 最新调度查询使用 `idx_dispatch_schedule_line_time`；
- `CHECK (schedule_code IS NULL OR schedule_code IN (-1,0,1))`。

市政府附近接口可能在多个站点条目中重复返回同一线路的调度信息。解析器必须在一次采集运行内按内部 `line_id` 去重，每条线路只创建一个调度快照。

### 4.11 `dispatch_cars` 发车明细表

| 字段 | 类型 | 空值 | 说明 |
|---|---|---:|---|
| `id` | BIGINT UNSIGNED | 否 | 主键，自增 |
| `schedule_id` | BIGINT UNSIGNED | 否 | 所属调度快照 |
| `sequence_no` | TINYINT UNSIGNED | 否 | API 数组顺序，从 1 开始 |
| `vehicle_text` | VARCHAR(64) | 是 | 上游车辆原始文本 |
| `is_barrier_free` | BOOLEAN | 是 | 从车辆文本或其他字段观测得到，可为空 |
| `planned_departure_at` | DATETIME(3) | 是 | 计划发车完整日期时间 |
| `countdown_text` | VARCHAR(64) | 是 | 如“剩余10分钟”“即将发车” |
| `countdown_seconds` | INT UNSIGNED | 是 | 可解析时保存数值，供分析使用 |

约束与索引：

- `PRIMARY KEY (id)`；
- `FOREIGN KEY (schedule_id) REFERENCES dispatch_schedules(id) ON DELETE CASCADE`；
- `UNIQUE KEY uq_dispatch_cars_schedule_sequence (schedule_id, sequence_no)`；该索引同时支持按计划读取全部明细；
- `KEY idx_dispatch_cars_departure (planned_departure_at)`，用于时间范围分析；
- `CHECK (sequence_no >= 1)`。

样例只给出 `HH:mm`。解析时以采集日期生成 `planned_departure_at`；若计划时间明显早于采集时间且符合跨午夜场景，则归入次日。无法可靠判定时字段置空，保留原始 `countdown_text` 并记录解析问题。

### 4.12 表关系汇总

```text
users 1 ---- N query_logs N ---- 1 stops
users M ---- N stops              via favorite_stops
lines M ---- N stops              via line_routes (包含顺序)
lines 1 ---- N arrival_infos N -- 1 stops
lines 1 ---- N dispatch_schedules 1 ---- N dispatch_cars
ingestion_runs 1 ---- N arrival_infos
ingestion_runs 1 ---- N dispatch_schedules
```

最终共 10 张表：`users`、`lines`、`stops`、`line_routes`、`favorite_stops`、`query_logs`、`ingestion_runs`、`arrival_infos`、`dispatch_schedules`、`dispatch_cars`。原报告中的 `Vehicle` 已按最终裁决删除。

### 4.13 各功能对应的 SQL 操作

以下 SQL 使用 MySQL 8 语法，是后端 Repository 层的实现模板，不表示前端可以直接提交 SQL。所有 `:name` 均为绑定参数，实际实现必须通过 SQLAlchemy 参数化执行，不得用字符串拼接用户输入。列表查询还应由后端限制 `LIMIT` 上限。

#### 4.13.1 用户注册、登录与管理

注册用户。`password_hash` 由后端完成哈希后写入：

```sql
INSERT INTO users
    (username, password_hash, role, is_active, created_at, updated_at)
VALUES
    (:username, :password_hash, 'passenger', TRUE, NOW(3), NOW(3));
```

登录时查询账户。密码验证在 Python 中进行：

```sql
SELECT id, username, password_hash, role, is_active
FROM users
WHERE username = :username
LIMIT 1;
```

管理员分页查看用户：

```sql
SELECT id, username, role, is_active, created_at, updated_at
FROM users
WHERE (:role IS NULL OR role = :role)
  AND (:is_active IS NULL OR is_active = :is_active)
ORDER BY id DESC
LIMIT :limit OFFSET :offset;
```

管理员修改角色或启用状态：

```sql
UPDATE users
SET role = :role,
    is_active = :is_active,
    updated_at = NOW(3)
WHERE id = :user_id;
```

删除用户不是常规管理功能；确需删除时，外键会自动删除收藏并把查询日志用户置空：

```sql
DELETE FROM users
WHERE id = :user_id;
```

后端必须禁止用户删除自己、最后一个有效管理员或越权修改更高权限账户。

#### 4.13.2 附近站点查询

后端先根据中心点和半径计算 `:min_lon`、`:max_lon`、`:min_lat`、`:max_lat`，用 B+ 树完成范围框预筛选，再使用 Haversine 公式精算距离：

```sql
SELECT candidate.id,
       candidate.stop_name,
       candidate.longitude,
       candidate.latitude,
       candidate.distance_m
FROM (
    SELECT s.id,
           s.stop_name,
           s.longitude,
           s.latitude,
           6371000 * 2 * ASIN(SQRT(
               POWER(SIN(RADIANS(s.latitude - :latitude) / 2), 2)
               + COS(RADIANS(:latitude)) * COS(RADIANS(s.latitude))
               * POWER(SIN(RADIANS(s.longitude - :longitude) / 2), 2)
           )) AS distance_m
    FROM stops AS s
    WHERE s.is_active = TRUE
      AND s.longitude BETWEEN :min_lon AND :max_lon
      AND s.latitude BETWEEN :min_lat AND :max_lat
) AS candidate
WHERE candidate.distance_m <= :radius_m
ORDER BY candidate.distance_m, candidate.id
LIMIT :limit;
```

项目数据量不超过 80 个站点时，也可在 SQL 范围框查询后由 Python 精算距离，但必须保持相同筛选语义。

#### 4.13.3 按名称搜索站点

精确或前缀搜索：

```sql
SELECT id, stop_name, longitude, latitude
FROM stops
WHERE is_active = TRUE
  AND stop_name LIKE CONCAT(:keyword, '%')
ORDER BY CASE WHEN stop_name = :keyword THEN 0 ELSE 1 END,
         stop_name,
         id
LIMIT :limit;
```

第一版不支持任意包含匹配 `%关键词%`，因为它无法有效使用当前 B+ 树索引。若本地无结果，后端再按更新策略调用高德站点接口。

#### 4.13.4 查看站点经过线路及方向

```sql
SELECT l.id,
       l.line_name,
       l.direction,
       l.line_type,
       l.first_departure_time,
       l.last_departure_time,
       lr.sequence_no
FROM line_routes AS lr
JOIN lines AS l ON l.id = lr.line_id
WHERE lr.stop_id = :stop_id
  AND l.is_active = TRUE
ORDER BY l.line_name, l.direction;
```

线路方向的起终点通过两次关联得到：

```sql
SELECT l.id,
       l.line_name,
       l.direction,
       first_stop.stop_name AS start_stop_name,
       last_stop.stop_name AS end_stop_name
FROM lines AS l
JOIN line_routes AS first_route
  ON first_route.line_id = l.id
 AND first_route.sequence_no = (
     SELECT MIN(x.sequence_no)
     FROM line_routes AS x
     WHERE x.line_id = l.id
 )
JOIN stops AS first_stop ON first_stop.id = first_route.stop_id
JOIN line_routes AS last_route
  ON last_route.line_id = l.id
 AND last_route.sequence_no = (
     SELECT MAX(y.sequence_no)
     FROM line_routes AS y
     WHERE y.line_id = l.id
 )
JOIN stops AS last_stop ON last_stop.id = last_route.stop_id
WHERE l.id = :line_id;
```

#### 4.13.5 查看线路完整站序和换乘线路

完整站序：

```sql
SELECT lr.sequence_no,
       s.id AS stop_id,
       s.stop_name,
       s.longitude,
       s.latitude
FROM line_routes AS lr
JOIN stops AS s ON s.id = lr.stop_id
WHERE lr.line_id = :line_id
  AND s.is_active = TRUE
ORDER BY lr.sequence_no;
```

某个站点可换乘的其他线路：

```sql
SELECT l.id, l.line_name, l.direction
FROM line_routes AS lr
JOIN lines AS l ON l.id = lr.line_id
WHERE lr.stop_id = :stop_id
  AND lr.line_id <> :current_line_id
  AND l.is_active = TRUE
ORDER BY l.line_name, l.direction;
```

#### 4.13.6 查看站点最新到站预测

使用窗口函数取得该站点每条线路的最新一条快照：

```sql
WITH ranked_arrivals AS (
    SELECT ai.*,
           ROW_NUMBER() OVER (
               PARTITION BY ai.line_id, ai.stop_id
               ORDER BY ai.collected_at DESC, ai.id DESC
           ) AS row_no
    FROM arrival_infos AS ai
    WHERE ai.stop_id = :stop_id
)
SELECT ra.id,
       ra.line_id,
       l.line_name,
       l.direction,
       ra.collected_at,
       ra.current_bus_distance_m,
       ra.current_bus_arrival_min,
       ra.current_bus_comfort,
       ra.current_bus_stop_count,
       ra.current_license_plate,
       ra.current_barrier_free,
       ra.next_bus_distance_m,
       ra.next_bus_arrival_min,
       ra.next_bus_stop_count,
       ra.next_license_plate,
       ra.next_barrier_free
FROM ranked_arrivals AS ra
JOIN lines AS l ON l.id = ra.line_id
WHERE ra.row_no = 1
  AND l.is_active = TRUE
ORDER BY l.line_name, l.direction;
```

若已经指定线路，则使用索引 `idx_arrival_realtime (line_id, stop_id, collected_at)` 反向扫描并直接查询：

```sql
SELECT *
FROM arrival_infos
WHERE line_id = :line_id
  AND stop_id = :stop_id
ORDER BY collected_at DESC, id DESC
LIMIT 1;
```

#### 4.13.7 查看最新计划发车信息

```sql
SELECT ds.id AS schedule_id,
       ds.line_id,
       ds.collected_at,
       ds.schedule_code,
       ds.message_default,
       ds.message_short,
       dc.sequence_no,
       dc.vehicle_text,
       dc.is_barrier_free,
       dc.planned_departure_at,
       dc.countdown_text,
       dc.countdown_seconds
FROM dispatch_schedules AS ds
LEFT JOIN dispatch_cars AS dc ON dc.schedule_id = ds.id
WHERE ds.id = (
    SELECT latest.id
    FROM dispatch_schedules AS latest
    WHERE latest.line_id = :line_id
    ORDER BY latest.collected_at DESC, latest.id DESC
    LIMIT 1
)
ORDER BY dc.sequence_no;
```

#### 4.13.8 收藏站点

添加收藏采用幂等写入：

```sql
INSERT INTO favorite_stops (user_id, stop_id, created_at)
VALUES (:user_id, :stop_id, NOW(3))
ON DUPLICATE KEY UPDATE created_at = created_at;
```

取消收藏：

```sql
DELETE FROM favorite_stops
WHERE user_id = :user_id
  AND stop_id = :stop_id;
```

查看收藏：

```sql
SELECT s.id, s.stop_name, s.longitude, s.latitude, fs.created_at
FROM favorite_stops AS fs
JOIN stops AS s ON s.id = fs.stop_id
WHERE fs.user_id = :user_id
  AND s.is_active = TRUE
ORDER BY fs.created_at DESC;
```

#### 4.13.9 记录查询日志

站点详情或实时查询成功返回后执行：

```sql
INSERT INTO query_logs (user_id, stop_id, queried_at)
VALUES (:user_id, :stop_id, NOW(3));
```

匿名查询时 `:user_id` 为 `NULL`。上游请求失败且本地也无可返回数据时不记录成功查询。

#### 4.13.10 预测候车时间趋势

按小时统计最近一辆车的平均预测到站分钟数：

```sql
SELECT DATE_FORMAT(ai.collected_at, '%Y-%m-%d %H:00:00') AS time_bucket,
       AVG(ai.current_bus_arrival_min) AS avg_predicted_wait_min,
       MIN(ai.current_bus_arrival_min) AS min_predicted_wait_min,
       MAX(ai.current_bus_arrival_min) AS max_predicted_wait_min,
       COUNT(ai.current_bus_arrival_min) AS sample_count
FROM arrival_infos AS ai
WHERE ai.line_id = :line_id
  AND (:stop_id IS NULL OR ai.stop_id = :stop_id)
  AND ai.collected_at >= :start_at
  AND ai.collected_at < :end_at
  AND ai.current_bus_arrival_min IS NOT NULL
GROUP BY time_bucket
ORDER BY time_bucket;
```

#### 4.13.11 计划发车间隔与稳定性

先在每个调度快照内按计划发车时间计算相邻车辆间隔，再按小时聚合：

```sql
WITH departure_intervals AS (
    SELECT ds.line_id,
           ds.collected_at,
           TIMESTAMPDIFF(
               MINUTE,
               LAG(dc.planned_departure_at) OVER (
                   PARTITION BY dc.schedule_id
                   ORDER BY dc.planned_departure_at, dc.sequence_no
               ),
               dc.planned_departure_at
           ) AS headway_min
    FROM dispatch_cars AS dc
    JOIN dispatch_schedules AS ds ON ds.id = dc.schedule_id
    WHERE ds.line_id = :line_id
      AND ds.collected_at >= :start_at
      AND ds.collected_at < :end_at
      AND dc.planned_departure_at IS NOT NULL
)
SELECT DATE_FORMAT(collected_at, '%Y-%m-%d %H:00:00') AS time_bucket,
       AVG(headway_min) AS avg_planned_headway_min,
       MIN(headway_min) AS min_planned_headway_min,
       MAX(headway_min) AS max_planned_headway_min,
       STDDEV_POP(headway_min) AS headway_stddev_min,
       COUNT(headway_min) AS sample_count
FROM departure_intervals
WHERE headway_min IS NOT NULL
  AND headway_min >= 0
GROUP BY time_bucket
ORDER BY time_bucket;
```

该结果只说明计划发车间隔，不代表车辆实际发车间隔或准点率。

#### 4.13.12 无障碍车辆观测比例

将当前和下一辆车视作独立观测样本：

```sql
WITH barrier_observations AS (
    SELECT line_id, collected_at, current_barrier_free AS is_barrier_free
    FROM arrival_infos
    WHERE current_barrier_free IS NOT NULL
    UNION ALL
    SELECT line_id, collected_at, next_barrier_free AS is_barrier_free
    FROM arrival_infos
    WHERE next_barrier_free IS NOT NULL
)
SELECT bo.line_id,
       l.line_name,
       l.direction,
       SUM(bo.is_barrier_free = TRUE) AS barrier_free_count,
       COUNT(*) AS observed_count,
       ROUND(SUM(bo.is_barrier_free = TRUE) / COUNT(*), 4) AS observed_ratio
FROM barrier_observations AS bo
JOIN lines AS l ON l.id = bo.line_id
WHERE bo.collected_at >= :start_at
  AND bo.collected_at < :end_at
  AND (:line_id IS NULL OR bo.line_id = :line_id)
GROUP BY bo.line_id, l.line_name, l.direction
ORDER BY observed_ratio DESC, observed_count DESC;
```

同一车辆可能在连续快照中重复出现，所以该结果是快照观测比例，不是去重后的车辆保有量比例。

#### 4.13.13 站点查询热度

```sql
SELECT ql.stop_id,
       s.stop_name,
       COUNT(*) AS query_count,
       COUNT(DISTINCT ql.user_id) AS known_user_count
FROM query_logs AS ql
JOIN stops AS s ON s.id = ql.stop_id
WHERE ql.queried_at >= :start_at
  AND ql.queried_at < :end_at
GROUP BY ql.stop_id, s.stop_name
ORDER BY query_count DESC, ql.stop_id
LIMIT :limit;
```

`known_user_count` 不包含匿名用户，`query_count` 表示系统查询次数而非客流量。

#### 4.13.14 线路和站点逻辑停用

```sql
UPDATE lines
SET is_active = FALSE,
    updated_at = NOW(3)
WHERE id = :line_id;

UPDATE stops
SET is_active = FALSE,
    updated_at = NOW(3)
WHERE id = :stop_id;
```

重新启用时把 `is_active` 更新为 `TRUE`。历史表不随逻辑停用删除。

#### 4.13.15 采集运行记录

上游调用前单独提交运行记录：

```sql
INSERT INTO ingestion_runs
    (source, task_type, trigger_type, request_key, started_at, status,
     received_count, inserted_count, updated_count, skipped_count, failed_count)
VALUES
    (:source, :task_type, :trigger_type, :request_key, NOW(3), 'running',
     0, 0, 0, 0, 0);
```

成功、部分成功或失败后更新：

```sql
UPDATE ingestion_runs
SET finished_at = NOW(3),
    status = :status,
    received_count = :received_count,
    inserted_count = :inserted_count,
    updated_count = :updated_count,
    skipped_count = :skipped_count,
    failed_count = :failed_count,
    error_message = :error_message
WHERE id = :ingestion_run_id;
```

进程异常后修复长期处于运行中的记录：

```sql
UPDATE ingestion_runs
SET status = 'failed',
    finished_at = NOW(3),
    error_message = COALESCE(error_message, '任务异常中止或进程退出')
WHERE status = 'running'
  AND started_at < NOW(3) - INTERVAL :timeout_minute MINUTE;
```

#### 4.13.16 高德线路、站点和站序同步

线路 upsert。方向必须在解析层确定后传入：

```sql
INSERT INTO lines
    (line_name, direction, line_type, amap_line_id,
     first_departure_time, last_departure_time,
     is_active, created_at, updated_at)
VALUES
    (:line_name, :direction, :line_type, :amap_line_id,
     :first_departure_time, :last_departure_time,
     TRUE, NOW(3), NOW(3))
ON DUPLICATE KEY UPDATE
    id = LAST_INSERT_ID(id),
    line_name = VALUES(line_name),
    line_type = VALUES(line_type),
    first_departure_time = VALUES(first_departure_time),
    last_departure_time = VALUES(last_departure_time),
    updated_at = NOW(3);
```

站点 upsert：

```sql
INSERT INTO stops
    (stop_name, amap_stop_id, longitude, latitude,
     is_active, created_at, updated_at)
VALUES
    (:stop_name, :amap_stop_id, :longitude, :latitude,
     TRUE, NOW(3), NOW(3))
ON DUPLICATE KEY UPDATE
    id = LAST_INSERT_ID(id),
    stop_name = VALUES(stop_name),
    longitude = VALUES(longitude),
    latitude = VALUES(latitude),
    updated_at = NOW(3);
```

解析器通过 `LAST_INSERT_ID()` 或 SQLAlchemy 刷新后的主键属性取得内部 ID。完整校验高德 `busstops` 后，在同一事务内重建站序。删除前先读取旧站序；仅当旧、新站序中的某个 `stop_id` 都只出现一次时，才把旧记录的 `shanghai_stop_id` 带入对应的新记录。重复经过同一物理站点或匹配不明确时保留 `NULL`，等待后续精确补全：

```sql
START TRANSACTION;

SELECT stop_id, sequence_no, shanghai_stop_id
FROM line_routes
WHERE line_id = :line_id;

DELETE FROM line_routes
WHERE line_id = :line_id;

-- 对解析后的每个站点执行参数化批量插入
INSERT INTO line_routes
    (line_id, stop_id, sequence_no, shanghai_stop_id)
VALUES
    (:line_id, :stop_id, :sequence_no, :shanghai_stop_id);

COMMIT;
```

任何站点解析、外键或唯一约束失败都必须 `ROLLBACK`，不得留下半条线路。

#### 4.13.17 市政府线路与线路站点标识补全

市政府线路按 API ID 和方向 upsert：

```sql
INSERT INTO lines
    (line_name, direction, line_type, shanghai_line_id,
     first_departure_time, last_departure_time,
     is_active, created_at, updated_at)
VALUES
    (:line_name, :direction, :line_type, :shanghai_line_id,
     :first_departure_time, :last_departure_time,
     TRUE, NOW(3), NOW(3))
ON DUPLICATE KEY UPDATE
    id = LAST_INSERT_ID(id),
    line_name = VALUES(line_name),
    line_type = VALUES(line_type),
    first_departure_time = VALUES(first_departure_time),
    last_departure_time = VALUES(last_departure_time),
    updated_at = NOW(3);
```

已经通过线路、站名、坐标和站序确认具体的 `line_routes.id` 后，补充关联上的市政府站点标识：

```sql
UPDATE line_routes
SET shanghai_stop_id = :shanghai_stop_id
WHERE id = :line_route_id
  AND shanghai_stop_id IS NULL;
```

不得只用 `(line_id, stop_id)` 更新，因为环线可能重复经过同一物理站点。若该站序已有不同的 `shanghai_stop_id`，不得覆盖，应把采集任务标记为 `partial` 并交由人工校对。

#### 4.13.18 写入到站快照

```sql
INSERT INTO arrival_infos
    (ingestion_run_id, line_id, stop_id, source_up_down, collected_at,
     current_bus_distance_m, current_bus_arrival_min,
     current_bus_comfort, current_bus_stop_count,
     current_license_plate, current_barrier_free,
     next_bus_distance_m, next_bus_arrival_min, next_bus_stop_count,
     next_license_plate, next_barrier_free)
VALUES
    (:ingestion_run_id, :line_id, :stop_id, :source_up_down, :collected_at,
     :current_bus_distance_m, :current_bus_arrival_min,
     :current_bus_comfort, :current_bus_stop_count,
     :current_license_plate, :current_barrier_free,
     :next_bus_distance_m, :next_bus_arrival_min, :next_bus_stop_count,
     :next_license_plate, :next_barrier_free)
ON DUPLICATE KEY UPDATE id = id;
```

`ON DUPLICATE KEY` 仅实现幂等跳过，不覆盖已经保存的历史观测。后端应通过受影响行数更新采集统计。

#### 4.13.19 写入调度快照和发车明细

同一线路在一次响应中去重后，先插入父记录：

```sql
INSERT INTO dispatch_schedules
    (ingestion_run_id, line_id, collected_at,
     schedule_code, message_default, message_short)
VALUES
    (:ingestion_run_id, :line_id, :collected_at,
     :schedule_code, :message_default, :message_short)
ON DUPLICATE KEY UPDATE id = LAST_INSERT_ID(id);
```

再对 `dispatchCars` 执行批量插入：

```sql
INSERT INTO dispatch_cars
    (schedule_id, sequence_no, vehicle_text, is_barrier_free,
     planned_departure_at, countdown_text, countdown_seconds)
VALUES
    (:schedule_id, :sequence_no, :vehicle_text, :is_barrier_free,
     :planned_departure_at, :countdown_text, :countdown_seconds)
ON DUPLICATE KEY UPDATE id = id;
```

父记录和所有明细必须位于同一业务事务中。采集运行记录使用独立事务，以便业务事务回滚后仍保留失败信息。

#### 4.13.20 数据质量检查

查找缺少站序或外部映射的有效线路：

```sql
SELECT l.id,
       l.line_name,
       l.direction,
       l.amap_line_id,
       l.shanghai_line_id,
       COUNT(lr.id) AS route_stop_count,
       SUM(lr.shanghai_stop_id IS NULL) AS missing_shanghai_stop_ids
FROM lines AS l
LEFT JOIN line_routes AS lr ON lr.line_id = l.id
WHERE l.is_active = TRUE
GROUP BY l.id, l.line_name, l.direction, l.amap_line_id, l.shanghai_line_id
HAVING route_stop_count = 0
    OR l.amap_line_id IS NULL
    OR l.shanghai_line_id IS NULL
    OR missing_shanghai_stop_ids > 0;
```

查找疑似重复物理站点，仅生成候选，不自动合并：

```sql
SELECT a.id AS stop_a_id,
       b.id AS stop_b_id,
       a.stop_name,
       a.longitude AS stop_a_longitude,
       a.latitude AS stop_a_latitude,
       b.longitude AS stop_b_longitude,
       b.latitude AS stop_b_latitude
FROM stops AS a
JOIN stops AS b
  ON a.id < b.id
 AND a.stop_name = b.stop_name
 AND ABS(a.longitude - b.longitude) <= :longitude_tolerance
 AND ABS(a.latitude - b.latitude) <= :latitude_tolerance
ORDER BY a.stop_name, a.id, b.id;
```

#### 4.13.21 历史数据清理

清理任务必须循环小批量执行并在每批后提交，避免长事务：

```sql
DELETE FROM arrival_infos
WHERE collected_at < NOW(3) - INTERVAL 90 DAY
ORDER BY collected_at
LIMIT 1000;

DELETE FROM dispatch_schedules
WHERE collected_at < NOW(3) - INTERVAL 90 DAY
ORDER BY collected_at
LIMIT 1000;

DELETE FROM query_logs
WHERE queried_at < NOW(3) - INTERVAL 180 DAY
ORDER BY queried_at
LIMIT 1000;

DELETE FROM ingestion_runs
WHERE status = 'success'
  AND started_at < NOW(3) - INTERVAL 180 DAY
ORDER BY started_at
LIMIT 1000;
```

删除 `dispatch_schedules` 时，外键会级联删除 `dispatch_cars`。失败和部分成功的采集记录不得套用成功记录的 180 天清理语句。

## 5. 数据库构建任务

### 5.1 初始化

1. 建立 MySQL 数据库、专用低权限用户和本地开发配置。
2. 设置 `utf8mb4`、InnoDB 和统一时区。
3. 建立 SQLAlchemy 模型及双向关系。
4. 使用 Alembic 生成并人工校对首个迁移。
5. 迁移中显式创建上述主键、唯一约束、外键、删除策略、检查约束和索引。
6. 建立管理员种子数据命令；密码从交互输入或环境变量读取，禁止写入仓库。

### 5.2 数据库校对

必须通过 `SHOW CREATE TABLE`、`SHOW INDEX` 和 `information_schema` 校对：

- 10 张表是否全部创建；
- 字段类型、可空性、默认值是否一致；
- 所有外键是否使用预期的 `CASCADE`、`SET NULL` 或 `RESTRICT`；
- `lines` 的市政府 ID 是否按 `(shanghai_line_id, direction)` 唯一；
- 快照唯一约束能否阻止重复导入；
- 中文站名、车牌和提示文本能否无损读写；
- `EXPLAIN` 是否使用附近站点、最新快照、线路站序和热度统计索引。

### 5.3 保留策略

课程数据量下先采用简单策略：

- `arrival_infos` 和 `dispatch_schedules` 默认保留 90 天；
- `dispatch_cars` 随父级调度快照清理；
- `query_logs` 默认保留 180 天；
- `ingestion_runs` 成功记录保留 180 天，失败和部分成功记录保留至少 1 年；
- 清理任务每日低峰运行，按时间索引分批删除，每批不超过 1000 行；
- 开发和演示阶段可关闭自动清理，但清理命令必须实现并可手动执行。

## 6. 后端搭建任务

### 6.1 项目结构

建议结构如下：

```text
backend/
  app/
    api/                 # REST 路由
    core/                # 配置、安全、日志
    db/                  # Session、Base、迁移辅助
    models/              # SQLAlchemy 模型
    schemas/             # Pydantic 请求/响应模型
    repositories/        # 明确的数据库查询
    services/            # 用户、站点、线路、统计等业务逻辑
    integrations/
      amap/              # 高德客户端、DTO、解析器
      shanghai/          # 市政府客户端、DTO、解析器
    jobs/                # 定时任务及清理任务
    main.py
  alembic/
  tests/
  pyproject.toml
```

路由层不得直接解析上游 JSON 或拼写复杂 SQL。外部 API 客户端、解析映射、数据库写入和业务响应分别放在独立模块中。

### 6.2 外部 API 适配规则

#### 市政府附近站点接口

样例根字段为 `nearByTrafficLineStop`。每个元素同时包含站点、线路、`sai` 到站预测和 `dispatchCarSchedule` 调度信息。

处理步骤：

1. 校验 `retCode` 和根数组；保存统一 `collected_at`。
2. 以 `(lineId, upDown)` 查找 `lines`；未匹配时可建立仅含市政府信息的线路记录。
3. 先以 `(line_id, stopId)` 查找已有 `line_routes` 关联；找不到时，再按规范化站名和坐标距离识别物理 `stops` 记录。
4. 找到对应的高德线路站序后，把市政府 `stopId` 补充到该 `line_routes.shanghai_stop_id`。尚无高德站序时不得伪造 `sequence_no`；可先保存物理站和实时快照，待高德线路同步后补充关联标识。
5. 名称和坐标只用于辅助匹配，不能覆盖已存在且冲突的外部 ID。
6. `sai` 存在时写入一条 `arrival_infos`；不存在时不伪造空快照。
7. 同一响应中的调度信息按内部线路去重，写入 `dispatch_schedules` 和对应 `dispatch_cars`。
8. 字符串数字统一安全转换为整数；空字符串、空数组、缺失键和非法值转换为 `NULL` 并计入跳过或数据质量统计。
9. API 返回的是给定半径内的部分站点线路组合，不得据此删除数据库中未出现在本次响应里的线路或站点。

#### 高德线路接口

样例根字段为 `buslines`，每个方向有独立 `id`，并包含 `start_time`、`end_time` 和有序 `busstops`。

处理步骤：

1. 每个 `buslines` 元素创建或更新一个 `lines` 方向记录；优先按 `amap_line_id` 匹配。
2. 从返回名称、起终点和 `direc` 辅助确定内部 `direction`。若无法可靠映射到 0/1，则任务标记为 `partial`，等待人工校对，不强行连接市政府 ID。
3. 按 `busstops[].id` upsert `stops`，解析 `location` 为经纬度。
4. 在一个事务中重建该线路的 `line_routes`；先完整校验新数组，再删除旧顺序并插入新顺序，避免只更新一半。
5. `start_time`、`end_time` 从 `HHmm` 解析为 `TIME`。
6. `polyline`、票价、公司等报告未要求的字段第一版不入库，避免扩大范围；需要地图线路轨迹时再通过接口实时获取或新增迁移。

#### 高德站点接口

样例根字段为 `busstops`，站点中嵌套 `buslines` 摘要。

处理步骤：

1. 按站名搜索只作为候选发现，不因同名自动合并全部结果。
2. 按 `amap_stop_id` 查询时可直接更新对应站点的名称和坐标。
3. 嵌套线路摘要仅用于发现待同步线路；完整站序必须再调用线路接口获取。
4. 多个同名站点通过高德 ID 和坐标区分。

### 6.3 双 API 映射策略

第一版不增加映射表，映射遵循以下顺序：

1. 已保存的高德线路/站点 ID、市政府线路方向 ID，或 `line_routes.shanghai_stop_id` 精确匹配。
2. 线路使用规范化线路名、方向、起终点组合生成候选。
3. 站点使用规范化站名和坐标距离阈值生成候选。
4. 候选唯一且满足规则时补充缺失的外部 ID。
5. 候选不唯一、坐标偏差过大或已有 ID 冲突时，不自动合并；保留独立记录并在 `ingestion_runs.error_message` 中写入摘要。

名称规范化只能移除稳定的格式差异，如首尾空白和统一括号，不应随意删除“临时站”“南广场”等具有业务含义的文本。

### 6.4 数据更新与 API 调用时机

#### 初始化阶段

1. 由管理员配置少量目标线路名、线路 ID 或种子坐标。
2. 先调用高德线路接口，建立 `lines`、`stops` 和 `line_routes` 静态骨架。
3. 再对种子坐标调用市政府附近接口，补充市政府线路/站点 ID、实时快照和调度快照。
4. 输出未匹配项目清单，人工确认后再次同步。

#### 高德静态数据

- 线路及站序：初始化时调用；之后每周一次或管理员手动触发。
- 站点按名/ID查询：用户搜索本地无结果、线路同步发现缺失站点、管理员手动补全时调用。
- 同一线路在 24 小时内已有成功同步记录时，普通用户请求不得重复触发同步。
- 高德接口失败时返回本地已有静态数据，并将采集运行标记为失败或部分成功。

#### 市政府实时数据

- 数据准备阶段：对配置的种子坐标每 20 分钟采集一次，与原报告一致。
- 用户查询附近站点或手动刷新：若该坐标网格最近 60 秒内已有成功响应，则复用最新数据库结果；否则调用上游接口并入库。
- 定时采集和用户触发可能重叠时使用进程内互斥锁；数据库唯一约束负责同一采集批次内的幂等，不能替代任务互斥。
- 每次调用只把 API 实际返回的记录视作本次观测，不推断附近所有站点均已完整返回。
- `scheduleCode=-1` 或 `0` 仍保存调度快照，但没有 `dispatchCars` 时不创建明细。

#### 清理与维护任务

- 每日执行一次历史数据分批清理；
- 每日扫描长时间处于 `running` 的采集记录并标记为失败；
- 每周输出外部 ID 缺失、线路无站序、线路站点关联缺少市政府 ID、站点坐标缺失或冲突等数据质量摘要；
- APScheduler 只在一个后端进程中启用。开发热重载和多 worker 部署时必须通过环境变量指定唯一调度器进程，避免任务重复执行。

### 6.5 REST API 规划

#### 认证与用户

```text
POST   /api/auth/register
POST   /api/auth/login
GET    /api/auth/me
GET    /api/admin/users
POST   /api/admin/users
PATCH  /api/admin/users/{id}
```

#### 站点、线路和实时信息

```text
GET    /api/stops/nearby?longitude=&latitude=&radius_m=
GET    /api/stops/search?q=
GET    /api/stops/{id}
GET    /api/stops/{id}/lines
GET    /api/stops/{id}/arrivals
POST   /api/stops/{id}/refresh
GET    /api/lines/{id}
GET    /api/lines/{id}/stops
GET    /api/lines/{id}/dispatch
```

附近站点接口使用经纬度范围框查询后，在 Python 中按 Haversine 距离精算和排序。必须限制 `radius_m` 最大值和返回数量。

#### 收藏和日志

```text
GET    /api/me/favorite-stops
POST   /api/me/favorite-stops/{stop_id}
DELETE /api/me/favorite-stops/{stop_id}
```

查询日志由后端在站点详情/实时查询服务成功返回时写入，不开放由前端任意提交日志的接口。

#### 分析接口

```text
GET /api/analytics/predicted-wait-times
GET /api/analytics/planned-headways
GET /api/analytics/barrier-free-observations
GET /api/analytics/stop-query-popularity
```

所有分析接口必须要求 `analyst` 或 `admin` 角色，必须限制最大时间范围，并使用数据库聚合和分页，不能一次加载全部历史数据到 Python。

#### 采集管理

```text
GET  /api/admin/ingestion-runs
GET  /api/admin/ingestion-runs/{id}
POST /api/admin/sync/amap-line
POST /api/admin/sync/amap-stop
POST /api/admin/sync/shanghai-nearby
```

### 6.6 后端测试与验收

1. 为三个样例 JSON 建立固定解析测试，验证缺失 `sai`、缺失 `dispatchCars`、只有一辆车、字符串数字和无障碍后缀。
2. 测试同一 JSON 重复导入不会生成重复快照。
3. 测试市政府同一 `lineId` 的两个方向能映射为两条内部线路。
4. 测试高德两个方向的线路和站序均正确写入。
5. 测试跨午夜的 `planned_departure_at` 处理。
6. 测试用户删除后查询日志保留且 `user_id` 置空，收藏记录删除。
7. 测试停用线路/站点不会出现在普通查询中，但历史快照仍可用于分析。
8. 测试乘客不能访问分析和管理接口，分析师不能管理用户。
9. 使用事务测试模拟中途异常，确认不会留下半条线路站序或孤立发车明细。
10. 对关键查询运行 `EXPLAIN` 并保存人工校对结果。

## 7. 前端搭建任务

### 7.1 页面

1. 登录/注册页。
2. 乘客主界面：定位、附近站点列表、站名搜索。
3. 站点详情：经过线路、最近两辆车、刷新、收藏。
4. 线路详情：方向、首末班时间、完整站序、首站计划发车。
5. 收藏站点页。
6. 分析页：四类指标的筛选器、表格和简洁图表。
7. 管理页：用户管理、采集触发、采集运行记录和错误详情。

### 7.2 前端原则

- 实时数据必须显示“最后更新时间”，过期数据要有明确状态；
- API 缺失字段显示“暂无数据”，不能显示 0 或 `false`；
- 上下行必须展示起终点辅助区分，不能只显示 0/1；
- 地图不是唯一入口，定位拒绝或地图加载失败时仍可按名称搜索；
- 手动刷新按钮应有加载、成功、失败和冷却状态；
- 根据后端角色控制路由，同时以后端 403 作为最终权限依据；
- 分析图表标题使用“预测候车时间”“计划发车间隔”“无障碍车辆观测比例”“站点查询热度”。

## 8. 实施阶段与交付物

### 阶段一：数据库与样例导入

- 创建后端项目、配置和数据库连接；
- 完成 10 张表的 SQLAlchemy 模型及 Alembic 迁移；
- 完成三个样例 JSON 的解析器和离线导入命令；
- 交付数据库结构校对结果和解析测试。

验收标准：空数据库可一条命令迁移完成；三个样例可导入；重复导入不产生重复快照；线路方向和站序正确。

### 阶段二：核心后端 API

- 完成认证、角色权限、站点、线路、到站预测、发车计划和收藏接口；
- 完成查询日志；
- 接入真实上游 API，并使用环境变量保存 URL、密钥和超时配置；
- 完成定时采集、短时缓存判断和采集运行记录。

验收标准：可通过 OpenAPI 页面完成乘客核心流程；上游失败时有明确错误且不破坏已有数据；每次采集均可追踪。

### 阶段三：分析和管理功能

- 完成四类分析查询；
- 完成管理员用户管理、手动同步、采集日志和清理命令；
- 对统计 SQL 和索引执行 `EXPLAIN` 校对。

验收标准：权限正确；统计名称和数据含义一致；指定时间范围内结果可复算。

### 阶段四：Vue 前端

- 完成乘客核心页面；
- 完成分析师和管理员页面；
- 处理加载、空数据、过期数据、错误和移动端布局；
- 与后端进行端到端联调。

验收标准：普通用户可完成搜索、附近查询、实时查看、线路查看和收藏；分析师可查看指标；管理员可查看采集状态。

### 阶段五：最终校对与文档

- 运行单元测试、集成测试和关键端到端测试；
- 校对迁移、索引、外键、接口文档与实际实现；
- 补充启动说明、环境变量模板、数据库初始化和演示数据导入说明；
- 根据最终实现修订课程报告中的 `Vehicle`、时间字段、分析指标和 `IngestionRun` 描述。

## 9. 人工校对清单

实施者每完成一个阶段，应由人工确认以下事项：

- [ ] 表结构与本文档一致，未擅自恢复 `Vehicle` 表；
- [ ] 两个方向使用不同内部 `line_id`；
- [ ] 外部 ID 允许为空，字符串前导零未丢失，市政府 `stopId` 存在 `line_routes` 而不是 `stops`；
- [ ] 没有仅凭同名强制合并站点；
- [ ] 市政府半径响应的缺失记录没有触发错误删除；
- [ ] 所有 API 可选字段能够安全处理 `NULL`；
- [ ] 发车时间已经转换为完整 `DATETIME`，跨日规则有测试；
- [ ] 线路和站点只逻辑停用，不物理删除；
- [ ] 查询日志在用户删除后保留并置空用户 ID；
- [ ] 快照唯一约束与应用层幂等逻辑同时生效；
- [ ] 每次外部 API 调用均有 `ingestion_runs` 记录；
- [ ] API 密钥、数据库密码和 JWT 密钥未提交到仓库；
- [ ] 权限由后端校验，不依赖前端；
- [ ] 分析指标没有被误称为真实客流、实际候车或实际准点率；
- [ ] 核心查询经过 `EXPLAIN`，索引确实被使用；
- [ ] README 中的初始化、迁移、启动、测试命令可在干净环境执行。

## 10. 暂不实施的内容

为控制课程项目难度，第一版明确不实施：

- 全上海所有线路和站点的完整镜像；
- Redis、Celery、Kafka、微服务或 Kubernetes；
- PostGIS/MySQL Spatial 空间索引；
- 基于实时数据推断真实客流或实际发车准点率；
- 第三辆及更多车辆的通用预测明细模型；
- 独立的双 API 映射审核表；
- 高德 `polyline`、票价、运营公司等非报告核心字段的长期存储。

若后续规模或功能要求变化，应通过新的 Alembic 迁移扩展，不直接修改已执行的历史迁移。
