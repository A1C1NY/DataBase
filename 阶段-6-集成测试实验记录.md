# 阶段六集成测试实验记录

记录日期：2026-08-06

## 1. 实验目标

本次只补充开发清单中缺少的新 `lineid` 流程验证，不重复已经完成的非法参数和非法检索测试。

验证内容：

- 高德线路 ID 查询无结果时返回 `404 NOT_FOUND_AFTER_AMAP`。
- Key 缺失、网络失败、超时时返回 `503 AMAP_UNAVAILABLE`。
- 高德业务错误返回 `502 AMAP_BUSINESS_ERROR`。
- 线路导入途中失败时，不留下半条线路、站序、轨迹或测试站点。
- 导入失败后仍保留 `endpoint='lineid'`、`status='failed'` 的审计记录及失败原因。
- `/api/stops/{stop_id}/lines` 只读数据库，不创建同步服务，也不发送 HTTP 请求。

## 2. 新增文件

- `backend/tests/integration/test_on_demand_sync.py`
- `backend/tests/integration/test_transit_routes.py`

同时在 `backend/pytest.ini` 注册了 `mysql_live` 标记，避免常规测试误访问当前数据库。

## 3. 数据库迁移状态

执行：

```bash
cd /home/alcony/桌面/design/DataBase/backend
../.database/bin/alembic current
```

实际结果：

```text
20260806_04 (head)
```

说明：当前库已经包含 `20260806_03` 的 `lineid` 枚举迁移，并继续升级到增加 `ui_color` 的 `20260806_04`。

## 4. Mock 与路由集成测试

执行：

```bash
cd /home/alcony/桌面/design/DataBase/backend
../.database/bin/pytest -q \
  tests/integration/test_on_demand_sync.py \
  tests/integration/test_transit_routes.py \
  -m 'not mysql_live'
```

实际结果：

```text
9 passed, 1 deselected in 0.43s
```

覆盖结果：

| 场景 | 断言结果 |
| --- | --- |
| `lineid` 返回空 `buslines` | `TransitNotFound`，HTTP 契约为 `404 / NOT_FOUND_AFTER_AMAP` |
| 未配置高德 Key | `503 / AMAP_UNAVAILABLE`，导入运行记录为失败 |
| HTTP 连接失败 | `503 / AMAP_UNAVAILABLE`，导入运行记录为失败 |
| HTTP 读取超时 | `503 / AMAP_UNAVAILABLE`，导入运行记录为失败 |
| 高德 `status=0` 业务错误 | `502 / AMAP_BUSINESS_ERROR`，导入运行记录为失败 |
| 站点经停线路查询 | 返回 `data_source=database`，同步服务调用次数严格为 `0` |
| FastAPI 路由错误映射 | 404、503、502 的 `detail.code` 均符合开发清单 |

“零 HTTP 请求”的证明方式是将路由模块的 `_sync()` 替换为一旦调用就立即失败的 Mock。测试成功返回数据库结果，并断言 `_sync()` 调用次数为 `0`，因此该接口没有构造高德客户端或发出上游请求。

## 5. 真实 MySQL 事务回滚测试

执行：

```bash
cd /home/alcony/桌面/design/DataBase/backend
TRANSIT_LIVE_ROLLBACK_TEST=1 \
  ../.database/bin/pytest -q \
  tests/integration/test_on_demand_sync.py::test_lineid_import_failure_rolls_back_business_rows_and_keeps_failed_run
```

实际结果：

```text
1 passed in 0.40s
```

实验过程：

1. 创建 `endpoint='lineid'` 的导入运行记录。
2. 使用专用虚构线路 ID `codex-stage6-rollback-line`，避免与现有业务数据冲突。
3. 在线路第二个站点写入时强制抛出异常，确保失败发生在数据库事务内部，而不是解析前。
4. 比较失败前后的 `bus_stops`、`bus_lines`、`bus_line_stops`、`bus_line_path_points` 总数。
5. 查询虚构线路，确认数据库中不存在该线路。
6. 查询导入运行，确认 `endpoint='lineid'`、`status='failed'`，且 `error_message` 包含真实失败原因。

实际断言全部通过：四张业务表计数没有变化，没有半条线路、测试站点、站序或轨迹残留；失败审计记录被保留。

## 6. 完整自动化回归

执行：

```bash
cd /home/alcony/桌面/design/DataBase/backend
../.database/bin/pytest -q
../.database/bin/ruff check .
git diff --check
```

实际结果：

```text
70 passed, 3 skipped in 0.68s
All checks passed!
git diff --check 无输出，退出码为 0
```

三个跳过项包括两个要求“专用 MySQL 测试库”的旧迁移测试，以及默认关闭的 `mysql_live` 回滚测试。回滚测试已经按第 5 节单独启用并成功执行。

## 7. 专用 MySQL 迁移套件

使用独立数据库 `transit_test` 执行：

```bash
cd /home/alcony/桌面/design/DataBase/backend
TRANSIT_TEST_DATABASE_URL='mysql+pymysql://user:***@127.0.0.1:3306/transit_test' \
  ../.database/bin/pytest -q -m mysql tests/mysql/test_stage_1_to_5_mysql.py
```

实际结果：

```text
2 passed in 1.20s
```

测试库成功从当前版本降级到 `base`，再升级到 `20260806_04 (head)`；样例重复导入保持幂等，既有冲突与回滚验证全部通过。

## 8. 最终结论

阶段六实现清单、手动流程、Mock 集成测试、真实 MySQL 回滚测试和专用数据库迁移套件均已通过。阶段六验收闭环完成，可以进入阶段七。
