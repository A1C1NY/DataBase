# 阶段 6 手动测试指南

本指南验证新的“站点摘要与线路详情分离”流程：

1. 站点搜索只在本地未命中时调用高德 `stopname`，保存站点和途经线路摘要。
2. 打开站点途经线路只读取数据库，不调用高德。
3. 用户点击某条未解析线路后，才使用 `amap_line_id` 调用高德 `lineid`。
4. 不再在站点线路页面按线路名称批量调用 `linename`。
5. 独立线路搜索在本地未命中时可按名称调用 `linename`。
6. 按高德线路 ID 查询会自动修复站序或轨迹不完整的本地记录，也可使用 `refresh=true` 强制刷新。

## 1. 准备环境

```bash
cd backend
source ../.database/bin/activate
python --version
python -c 'import fastapi, sqlalchemy, pymysql, alembic, httpx; print("依赖可用")'
```

测试必须使用数据库名称包含 `test` 的 MySQL 8 数据库。确认当前连接目标：

```bash
python - <<'PY'
from app.core.config import get_settings
from sqlalchemy.engine import make_url

url = make_url(get_settings().database_url)
print(url.render_as_string(hide_password=True))
assert url.database and "test" in url.database.lower()
PY
```

## 2. 应用数据库迁移

```bash
alembic upgrade head
alembic current
```

预期当前版本：

```text
20260806_04 (head)
```

在 MySQL 中确认新结构：

```sql
SHOW COLUMNS FROM bus_stops LIKE 'unresolved_line_summaries';
SHOW COLUMNS FROM ingestion_runs LIKE 'endpoint';
SHOW COLUMNS FROM bus_lines LIKE 'ui_color';
```

预期：

- `bus_stops.unresolved_line_summaries` 为可空 JSON；
- `ingestion_runs.endpoint` 支持 `stopname`、`linename`、`lineid`。
- `bus_lines.ui_color` 为可空 `VARCHAR(16)`，高德 `uicolor=[]` 写入 `NULL`。

## 3. 启动 API

```bash
uvicorn app.main:app --host 127.0.0.1 --port 8000
```

另开终端：

```bash
curl -sS http://127.0.0.1:8000/health
curl -sS http://127.0.0.1:8000/openapi.json > /tmp/stage6-openapi.json
```

预期 `/health` 返回：

```json
{"status":"ok"}
```

OpenAPI 应包含：

- `/api/stops/search`
- `/api/stops/{stop_id}/lines`
- `/api/lines/search`
- `/api/lines/by-amap/{amap_line_id}`
- `/api/lines/{line_id}`
- `/api/lines/{line_id}/stops`
- `/api/lines/{line_id}/map`

`/api/lines/by-amap/{amap_line_id}` 不再要求 `line_name` 或 `city_code`。

## 4. 首次搜索站点并保存线路摘要

选择一个当前数据库中不存在的上海公交站名称。先在 MySQL 中确认没有本地记录：

```sql
SELECT id, stop_name
FROM bus_stops
WHERE normalized_name LIKE '%待测试站名%';
```

执行搜索：

```bash
curl -sSG 'http://127.0.0.1:8000/api/stops/search' \
  --data-urlencode 'q=待测试站名' \
  --data-urlencode 'city_code=021' \
  --data-urlencode 'limit=20' \
  | python -m json.tool
```

预期首次响应：

- HTTP `200`；
- `data_source="amap"`；
- 包含 `ingestion_run_id`；
- `items` 中包含站点数据库数字 `id`。

数据库验证：

```sql
SELECT id, amap_stop_id, stop_name, line_membership_status,
       JSON_LENGTH(unresolved_line_summaries) AS unresolved_count,
       last_ingestion_run_id
FROM bus_stops
WHERE id = 实际站点ID;

SELECT id, endpoint, request_keyword, status
FROM ingestion_runs
WHERE id = 搜索响应中的ingestion_run_id;
```

预期：

- 站点已经写入 `bus_stops`；
- 线路摘要写入 `unresolved_line_summaries`；
- 有线路摘要时状态为 `partial`；
- 导入记录 `endpoint='stopname'`；
- 此步骤不批量写入完整 `bus_lines`、站序或轨迹。

再次执行相同搜索，预期 `data_source="database"`，且不新增导入记录。

## 5. 打开站点线路列表不得访问高德

调用前记录当前最大导入 ID：

```sql
SELECT MAX(id) AS before_run_id FROM ingestion_runs;
```

请求站点线路列表：

```bash
curl -sS 'http://127.0.0.1:8000/api/stops/实际站点ID/lines' \
  | python -m json.tool
```

预期：

- `data_source="database"`；
- `lines` 只包含已经完整入库的线路；
- 尚未导入的线路位于 `unresolved_summaries`；
- `partial=true` 时也不调用高德；
- `unresolved_summaries` 中包含 `amap_line_id`、线路名称和起终点。

调用后再次检查：

```sql
SELECT MAX(id) AS after_run_id FROM ingestion_runs;
```

`before_run_id` 与 `after_run_id` 必须相同。重复打开站点线路列表也不得新增 `stopname`、`linename` 或 `lineid` 记录。

## 6. 点击一条线路后按线路 ID 补齐

从 `unresolved_summaries` 选择一个 `amap_line_id`。先确认该方向尚未入库：

```sql
SELECT id, amap_line_id, line_name
FROM bus_lines
WHERE amap_line_id = '310100024547';
```

调用线路 ID 补数接口：

```bash
curl -sS 'http://127.0.0.1:8000/api/lines/by-amap/310100024547' \
  | python -m json.tool
```

预期首次响应：

- HTTP `200`；
- `data_source="amap"`；
- 包含 `ingestion_run_id`；
- 返回完整线路信息和内部数字 `line.id`。

数据库验证：

```sql
SELECT id, endpoint, request_keyword, status
FROM ingestion_runs
WHERE id = 70;

SELECT id, amap_line_id, line_name, start_stop_name, end_stop_name
FROM bus_lines
WHERE amap_line_id = '310100024547';

SELECT COUNT(*) AS stop_count
FROM bus_line_stops
WHERE line_id = 19;

SELECT COUNT(*) AS path_point_count
FROM bus_line_path_points
WHERE line_id = 19;
```

预期：

- 导入记录 `endpoint='lineid'`；
- `request_keyword` 保存高德线路 ID，而不是线路名称；
- 完整线路、站序和轨迹已经入库；
- 对应站点的该条摘要从 `unresolved_line_summaries` 中移除。

再次请求站点线路列表：

```bash
curl -sS 'http://127.0.0.1:8000/api/stops/实际站点ID/lines' \
  | python -m json.tool
```

预期新线路出现在 `lines`，`unresolved_summaries` 数量减少一条。只要仍有未点击线路，站点继续保持 `partial`，系统不会自动补齐它们。

再次请求同一个线路 ID：

```bash
curl -sS 'http://127.0.0.1:8000/api/lines/by-amap/实际高德线路ID' \
  | python -m json.tool
```

预期 `data_source="database"`，且不新增导入记录。

## 7. 线路详情、站序和地图

使用上一步返回的内部数字 `line.id`：

```bash
curl -sS http://127.0.0.1:8000/api/lines/19 \
  | python -m json.tool
curl -sS http://127.0.0.1:8000/api/lines/19/stops \
  | python -m json.tool
curl -sS http://127.0.0.1:8000/api/lines/19/map \
  | python -m json.tool
```

预期：

- 三个接口均为 `data_source="database"`；
- 站序从 `1` 连续；
- 地图为 `FeatureCollection`；
- 包含一条 `LineString` 和有序站点 `Point`；
- 坐标元数据标明 `GCJ02 -> WGS84`。

## 8. 导入记录验收

```sql
SELECT id, endpoint, request_keyword, status,
       received_count, inserted_count, updated_count,
       skipped_count, failed_count, error_message
FROM ingestion_runs
ORDER BY id DESC
LIMIT 20;
```

新的完整流程应表现为：

1. 首次未命中站点搜索产生一条 `stopname`；
2. 打开或重复打开站点线路列表不产生导入记录；
3. 每点击一个未解析线路方向，只产生一条 `lineid`；
4. 同一线路再次点击直接命中数据库；
5. 此流程不产生按线路名称查询的 `linename` 记录。

## 9. 阶段 6 完成标准

- 数据库优先搜索有效；
- `stopname` 保存站点和线路摘要，但不批量导入完整线路；
- 站点线路列表始终只读数据库；
- 用户点击具体摘要后才调用 `lineid`；
- 完整线路入库后只移除对应摘要；
- 未点击线路保持原状，不自动访问高德；
- 每次真实上游请求都有独立导入记录；
- 第二次访问相同站点或线路直接命中数据库。

## 10. 2026-08-06 实际验证记录

本次使用 `amap_transit_test`；线路流程在 `20260806_03` 验证完成，随后升级到 `20260806_04 (head)` 增加 `ui_color`。

测试站点：

```text
剑川路沧源路(公交站)
内部站点 ID: 228
高德站点 ID: BV10036943
```

首次站点搜索结果：

- `data_source="amap"`；
- 导入运行 `id=67`、`endpoint='stopname'`、`status='success'`；
- 保存4条未解析线路摘要；
- 站点状态为 `partial`。

打开 `/api/stops/228/lines`：

- `data_source="database"`；
- `lines=[]`；
- `unresolved_summaries` 为4条；
- 调用前后 `MAX(ingestion_runs.id)` 均为 `67`。

点击线路摘要 `amap_line_id=900000027120`：

- 调用 `/api/lines/by-amap/900000027120`；
- 首次返回 `data_source="amap"`；
- 导入运行 `id=68`、`endpoint='lineid'`、`status='success'`；
- 内部线路 ID 为 `18`；
- 导入21个有序站点和233个轨迹点；
- 站点228的未解析摘要从4条减少为3条；
- 其余3条线路未自动导入，站点继续保持 `partial`。

重复验证：

- 再次访问相同 `amap_line_id` 返回 `data_source="database"`；
- 线路详情、站序和地图均返回 `data_source="database"`；
- 站序范围为 `1-21`；
- 地图包含 `LineString` 和 `Point`，元数据为 `GCJ02 -> WGS84`；
- 最终 `MAX(ingestion_runs.id)=68`；
- 新流程只产生一条 `stopname` 和一条 `lineid`，没有产生 `linename`。
